# SGO Beija Flor — Contexto do Projeto

> Lido automaticamente pelo Claude Code no início de cada sessão. Mantenha atualizado — especialmente a tabela de status e o mapa do ambiente.

## O que é
Sistema de Gestão Operacional (SGO) para rede de restaurantes/churrascarias (6–15 unidades). Especificação completa: `docs/especificacao.md` — leia a seção do módulo antes de codar.

## 🚚 MIGRADO PARA O DROPLET EM 22/07/2026 — leia isto antes de qualquer deploy
A produção do SGO **NÃO roda mais nesta máquina Windows**. Desde 22/07/2026 (~01:35 UTC) roda no
**droplet `174.138.88.225`** (DigitalOcean, 2 vCPU / 8 GB / 154 GB), **ao lado da plataforma do CEO**:

| | |
|---|---|
| Acesso | `ssh -i ~/.ssh/bjf_vps root@174.138.88.225` · app em `/opt/sgo/app` |
| Containers | `sgo_app` (127.0.0.1:3100, nenhuma porta pública) + `sgo_postgres` (sem porta), rede `sgo` |
| Publicação | **Caddy do CEO** (`bjf_caddy`, `/opt/bjf/Caddyfile`) → `reverse_proxy sgo_app:3100`; TLS Let's Encrypt automático. **Não há mais Cloudflare Tunnel no caminho do SGO** — DNS é registro **A → 174.138.88.225, DNS only (cinza)**, apex + www |
| Build | **passa no droplet** (o que estourava a RAM no Windows). `docker build -t sgo-sgo-app:vX.Y.Z .` em `/opt/sgo/app`, `docker tag … latest`, `docker compose -f docker-compose.prod.yml up -d --no-build` |
| Migrações | container descartável na rede `sgo` com `prisma migrate deploy` (ver [[prod-migrations]]) |
| Backup | `/opt/sgo/backup-sgo.sh` + cron 06:00 UTC (03:00 BRT), retenção 14 dias — **no mesmo disco do banco**. ⚠️ **Sem cópia fora da máquina até hoje**: se o droplet se perder, os 14 dias vão junto. Espelho pronto para aplicar em `scripts/mirror-backup-rclone.sh` (**não instalado** — falta criar o remote). **Conta separada, decidido 15/08/2026**: o `gdrive:` do droplet é do CEO e dado de funcionário (CPF/PIX/CID) não passa por lá; o script aborta se apontarem para ele |
| Swap | `+4 GB` em `/swapfile-sgo` (ativo, **fora do fstab** — some no reboot; recriar antes de builds grandes) |
| Rollback | máquina Windows **intacta**: `sgo_postgres` no ar, volumes preservados, `sgo_app` **parado** (evita scheduler duplicado). Reverter = `docker start sgo_app` + recriar os CNAME de Tunnel no DNS |

⚠️ Ao mexer no Caddy: backup do Caddyfile, `caddy validate` e `caddy reload` (gracioso). **Restart do `bjf_caddy` derruba o CEO por ~2s** — só quando necessário (foi preciso 1× para zerar o backoff de certificado, que o reload não limpa).

O mapa abaixo descreve a **máquina Windows**, que segue rodando o que sobrou (Postgres do SGO congelado p/ rollback). A plataforma do CEO também já vive no droplet.

## ⚠️ Ambiente da máquina Windows (inspeção Fase 0.1 — 2026-06-10; histórico)
Esta máquina (Windows 10 Pro) **já roda em produção a plataforma do CEO ("Beija Flor Platform") — INTOCÁVEL**. Mapa do que NÃO pode ser conflitado:

| Recurso do CEO | Porta / Local |
|---|---|
| Frontend Next.js (CEO) | `localhost:3000` (node, `Desktop\beija-flor-platform\apps\web`) |
| API NestJS (CEO) | `localhost:3001` (`/v1/*`) |
| WhatsApp Evolution API | container `bjf_evolution` → `9000` (`/wa/*`) |
| PostgreSQL do CEO | container `bjf_postgres` → **`5432`** (`bjf_user`/`bjf_platform`) |
| Redis do CEO | container `bjf_redis` → `6379` |
| Stack Docker do CEO | `C:\Users\User\bjf\docker-compose.yml` (projeto `bjf`) |
| Publicação | **Cloudflare Tunnel** `cloudflared` (PID/serviço), config `C:\Users\User\.cloudflared\config.yml`, domínio `bjf-plataforma.com.br` |

**Portas/recursos do SGO (exclusivos, sem conflito):** app `3100` (prod) / `3101` (homolog) · Postgres dedicado `sgo_postgres` (host dev `5433`, em prod sem porta publicada) · rede docker `sgo` · volumes `sgo_db_data`/`sgo_uploads`.

**Publicação:** ✅ NO AR desde 2026-06-12 em **https://sgorestaurantesgbf.com.br** (+ www). Produção via Docker (`docker-compose.prod.yml`: `sgo_app` em 127.0.0.1:3100, `sgo_postgres` sem porta exposta, `restart: unless-stopped`). Publicado pelo **mesmo túnel Cloudflare do CEO** (id `095cb96a…`, tunnel "beija-flor"): ingress do SGO adicionado em `~/.cloudflared/config.yml` (rotas do CEO intactas; backup em `config.yml.bak-pre-sgo`); DNS = CNAME proxied de apex+www → `095cb96a….cfargotunnel.com` (criados no painel — o cert.pem do cloudflared só gerencia a zona do CEO).
**Operação do túnel (cuidado):** rode SEMPRE só UMA instância do cloudflared (binário real em `...WinGet\Packages\Cloudflare.cloudflared_*\cloudflared.exe`, NÃO o shim de `WinGet\Links`). Use `127.0.0.1` (não `localhost`) nas rotas do SGO (app é IPv4-only; `localhost`→::1 dá 502). Reiniciar o cloudflared derruba o CEO por ~5s.
**Auto-start no boot (✅ 2026-06-15):** o stack inteiro sobe por **logon** (não pré-login). Pasta `shell:startup` do usuário contém: `Docker Desktop.lnk` (+ flag nativo `AutoStart:true` em `%APPDATA%\Docker\settings-store.json`) → engine sobe e containers `restart:unless-stopped` voltam; `cloudflared-tunnel.vbs` → chama `~/.cloudflared/start-tunnel.cmd` (lança o binário real OCULTO, com **guarda de instância única** via tasklist) → túnel; `BeijaFlor Platform.lnk` (autostart do CEO, **não mexer**). Reboot exige login do usuário p/ tudo voltar. Diag pós-reboot: 1033=cloudflared off; 502 no SGO=Docker/app off; 502 no CEO=node do CEO (porta 3000) off — responsabilidade do CEO.
**Pendências de produção:** trocar senhas de demonstração antes de liberar usuários; rotacionar RH_API_KEY (exposta em prints) e chave Anthropic — passo a passo em `docs/pendencias-producao.md`; ~~backup 3-2-1 COMPLETO~~ — **valia para a era Windows e foi DESFEITO na migração** (verificado 15/08/2026): a tarefa `sgo-backup` não existe mais, `BACKUP_MIRROR_DIR` está vazio no `.env`, não há pasta `SGO Backups` no Drive nem dump algum no G:. Hoje o backup vive **só no droplet** — ver a linha Backup no mapa acima; PWA/FCM ainda pendente. ⚠️ Disco C: ~95% cheio (compactar VHDX do Docker exige parar Docker = derruba CEO — só combinado).

## Stack
Next.js 14 full-stack + TS + Tailwind + shadcn/ui · PostgreSQL 16 (Docker) · JWT+refresh+bcrypt · Prisma · FCM + Central in-app · PWA · Claude API (`@anthropic-ai/sdk`, modelo via `CLAUDE_MODEL`, chave `ANTHROPIC_API_KEY` — visão usada na checagem de foto×padrão dos checklists, `src/lib/ai/vision.ts`, inerte sem chave)

## Regras inegociáveis (nunca viole)
1. **NUNCA alterar/derrubar/conflitar com a plataforma do CEO.** Ver mapa acima. Mudanças no proxy/túnel só com confirmação explícita.
2. Multi-seleção (unidades/pessoas) usa o componente `MultiSelect` (`src/components/ui/multi-select.tsx`) — caixa de seleção compacta com busca+checkboxes, não mais "chips" soltos. Interface 100% PT-BR, mobile-first, tema claro. **Cores das churrascarias (desde 2026-06-11): bordô `#6E1423` + cinza escuro `#3F3F46`** (token tailwind `gold` = grafite, nome mantido por compatibilidade). Semáforos de status continuam verde/âmbar/vermelho.
3. Escopo por unidade (`unit_id`) SEMPRE no servidor.
4. Lógica diária usa **data operacional** (corte por unidade, padrão 04:00).
5. Tarefas pelo **calendário de operação** (fins de semana incluídos), nunca "dias úteis".
6. Modelo Claude API por env — nunca fixo no código.
7. Ações críticas no Log de Auditoria (imutável).
8. Conclusão de tarefas transacional (sem duplicidade).
9. Nenhuma porta de container publicada direto na internet — só o proxy/túnel.
10. Segredos em `.env` fora do Git.

## Status
| Fase / Módulo | Status |
|---|---|
| Fase 0 — Infraestrutura | ✅ Concluído (compose dev/prod, Dockerfile, backups, /api/health) |
| 1 — Auth + Estrutura base | ✅ Concluído (JWT+refresh+bcrypt, perfis, multi-unidade, escopo no servidor, data operacional, 10 testes verdes) |
| 0 (UI) — Dashboard | ✅ Concluído (gerente: anel+meta+atalhos; CEO/Admin/Supervisor: semáforo+ranking+alertas; refresh 60s, skeletons) |
| 1 — Checklists | ✅ Concluído (geração por dia operacional, conclusão transacional, evidência fotográfica, "não realizada" automática, 17 testes; **editar unidades onde o checklist aparece** via `groupKey` (`template.setUnits`); **Admin exclui histórico** de realizados/não em Tarefas→Histórico (`taskInstance` em /api/admin/ops); **programação `startDate`/`endDate`** no template (geração só dentro do período); **exclusão com `force`** apaga template+histórico (checklists de teste); detalhe da tarefa mostra fotos de `TaskPhoto` **+ `evidencePath`** (conclusões legadas); 1-por-unidade (scope UNIT) conclui 1×/dia (updateMany atômico→ALREADY_DONE, tela concluída só leitura); **item c/ ocorrência aberta sinalizado nos dias seguintes** sem duplicar pendência (`Occurrence.sourceTaskItemId`) (v1.20.0)) |
| 2 — Desperdícios | ✅ Concluído (1 lançamento/dia, conclui tarefa WASTE c/ foto da balança, alerta >20% vs média 7d, mini-dashboard barras/% e comparativo entre unidades; **histórico clicável c/ itens**, **categorias em Config (CRUD)**, **lançar dia anterior** via seletor de data, **export Excel/CSV** `/api/waste/export`; **medida un/kg por categoria** (`WasteCategory.measure`, lanchonete em unidades) + **sub-itens** somando o total (`WasteEntryItem.subItems`) (v1.19.0)) |
| 3 — Comandas | ✅ Concluído (contagem diária "todas presentes"/ausentes, divergência+alerta supervisor, ciclo OPEN→apuração→encerrada recuperada/perdida, baixa sai da ativa, reposição admin; **várias sequências por unidade** — modelo `CommandSequence`, Config CRUD, `getActiveSequence` soma todas as faixas ativas; `UnitCommandConfig` legado; **conferência em grade** (v1.3.3): gerente toca cada comanda conferida (verde), não marcadas = faltando → `absent = ativas − selecionadas`, reusa `/api/commands/count`; atalho "Todas presentes" + manual recolhido; **seleção por faixa em lote** (marca/desmarca de X a Y) v1.5.0; **grade 3 estados** (v1.18.0): 1 toque=conferida, 2=em uso (conta presente), 3=limpa; em apuração/perdidas SAEM da grade (`underReview` via divergências OPEN); **conferência por LEITOR de código de barras** (v1.39.0): perfil **CAIXA** (`Role.CASHIER`, nasce fechado via `DEFAULT_ALLOW_ONLY` — só Comandas+Ajuda, cai direto na tela), `/modulos/comandas/conferencia` com campo focado (leitor = teclado), faltantes = ativas − bipadas reusando `submitCount`, parser tolerante `src/lib/commands/barcode.ts` (a calibrar com exemplo real) e **cruzamento com a última análise de comandas em aberto** = faltante + aberta com valor → suspeita de fraude das "2 comandas") |
| — Comandas: o leitor travava a justificativa (v1.109.1) | ✅ Concluído. 🔴 **Em produção, em TODA conferência com leitor**: ao clicar em "Finalizar conferência" e tentar preencher **"O que houve? (obrigatório)"**, o campo não aceitava texto; como a justificativa é obrigatória, o botão de finalizar ficava inalcançável e a conferência travava (no print: 423 de 504, 84%, com 81 não localizadas). Causa numa linha só, em `sessao-client.tsx`: `onBlur={() => setTimeout(() => inputRef.current?.focus(), 50)}` no campo do leitor — ele recapturava o foco 50ms depois de perdê-lo **sem exceção**, inclusive quando o foco tinha ido para um campo do diálogo sobreposto. Agora `leitorAtivo = usaLeitor && podeEditar && !fechando` é a **condição ÚNICA**, usada no efeito de foco E no `onBlur` (duas condições em lugares diferentes é como uma envelhece sozinha). ⚠️ A condição é lida de uma **REFERÊNCIA** (`leitorAtivoRef.current`), e não da variável: com a variável valeria o estado do instante em que o timeout foi AGENDADO — ao tocar em "Finalizar conferência" o campo perde o foco no mesmo momento em que o diálogo abre, e aquele timeout ainda veria "ativo", roubando o foco de volta. A justificativa **continua obrigatória** (a correção é sobre poder escrever, não sobre deixar de exigir). 7 testes de FONTE, 3 verificados **reprovando** a linha anterior — teste de fonte e não de tela porque `jsdom` não reproduz disputa de foco entre campo e diálogo sobreposto, e um teste que "passa" sem reproduzir o defeito dá falsa segurança |
| 4 — Cancelamento de Cupons | ✅ Concluído (import CSV Teknisa genérico, pendência de justificativa, motivos configuráveis, mês/% justificados/ranking operador, badge dashboard; **relatório PDF (print) + Excel** `/api/cancellations/export` e `/modulos/cancelamentos/relatorio` por mês/unidade) |
| 5 — Inventário | ✅ Concluído. **Seção 1 — Teknisa**: agendamento Admin + confirmação do gerente (acompanhamento da tarefa). **Seção 2 — Equipamentos** (módulo completo, fora do Teknisa): `InventoryItem` (nome, fornecedor, unidade, valor, estoque mínimo, local) + `InventoryMovement` (IN/OUT/ADJUST atualizam saldo); estoque com valor total e alerta de mínimo, movimentação entrada/saída, **contagem com ajuste + folha de contagem imprimível**, histórico; `/api/inventory-equip`, `src/lib/inventory-equip.ts`. Admin exclui item sem movimento |
| 6 — Ocorrências | ✅ Concluído (nº sequencial/unidade, tipo→categoria, gravidade 🟢🟡🔴⚫, anexos foto/vídeo, reincidência <30d, encerramento Supervisor/Admin com ação corretiva, alertas e badge no dashboard; **tipos/categorias em Config (CRUD)**, gravidade fixa em 4 níveis; **relatório PDF por ocorrência** (`[id]/relatorio`, print A4) p/ WhatsApp; **sub-aba Manutenção** filtra tipos `isMaintenance` e leva ao módulo Manutenção; **sub-aba TI** (v1.15.0) filtra tipos `isIT` — preparo p/ futura API de gestão de TI; Manutenção saiu da sidebar (acesso por aqui); **fases de andamento** (`OccurrenceUpdate`, timeline no detalhe) + **reclassificar tipo/categoria** Sup/Admin (move entre abas Manutenção/TI, auditado) (v1.20.0)) |
| 6.1 — Manutenção (v1.4.0) | ✅ Concluído (`/modulos/manutencao`, `src/lib/maintenance.ts`). **Chamados**: `MaintenanceTicket` (nº/unidade, equipamento+prestador+prazo snapshot, status OPEN→IN_PROGRESS→DONE/CANCELED, custo, resolução; painel abertos/andamento/atrasados/mês+custo; notifica supervisão na abertura; `/api/maintenance/tickets`). **Preventiva**: `MaintenancePlan` + `MaintenancePlanLog` (recorrência em dias por equipamento, nextDueAt, registrar execução agenda a próxima+histórico; scheduler `runDueMaintenancePlans` avisa gerente/supervisão dos vencidos 1×/dia via `lastNotifiedAt`; `/api/maintenance/plans`). Escopo denormalizado (sem relations, snapshot de nomes). Admin exclui via `/api/admin/ops` (`maintenanceTicket`/`maintenancePlan`). Módulo `MAINTENANCE` em permissions + sidebar) |
| 7 — Pagamentos (com delegação) | ✅ Concluído (freelancers/HE/avulsos, fluxo solicitar→aprovar→pagar, delegação de aprovador por período, abas Minhas/Aprovar/Pagar/Histórico, badges dashboard; **Admin edita valor/descrição e exclui** lançamentos no histórico, auditado; **freelancer com chave PIX obrigatória** + valor padrão (Config); **divergência de valor** vs padrão no lançamento (snapshot `standardValue`+`divergent`, alerta gerente/aprovadores, não bloqueia); **relatório mensal de consolidação** de freelancers (`relatorio-freelancers`, PIX+total, PDF/print + Excel `/api/payments/freelancer-report`); **data da solicitação visível + histórico ordenado por ela** e **edição de data por Admin/Supervisor** c/ desconto na meta (v1.15.0, `/api/entry-date`); **cobertura temporária de setor** p/ freelancer (valor/dia por setor `FreelancerSectorRate` + VT, Config), **VT na Hora Extra**, **consolidação semanal seg→dom** `/modulos/pagamentos/semanal` (v1.19.0); **v1.69.0**: freelancer **nasce alocado** — `workDate` + `workSectorId` obrigatórios no lançamento (setor da unidade, validado em `createPaymentRequest`), o Mapa lê direto; **aprovador corrige antes de aprovar** (`approverEditRequest` em `payments/approve.ts`, ação `approverEdit`, mesma porta da aprovação, recalcula valor/hora e divergência, auditoria `PAYMENT_APPROVER_EDIT` c/ antes/depois, avisa solicitante; botão "Editar" no detalhe da aba Para Aprovar); `/api/payments/[id]` com **guarda por aba** (approve/reject/approverEdit→Aprovar, pay→Pagar); **v1.75.0**: **alerta de recorrência** — mesmo freelancer > `FREELANCER_WEEK_LIMIT` (AppSetting, padrão 2, Admin edita em Config→Pagamentos) na semana seg→dom do `workDate`, rede inteira, sem rejeitadas → `PaymentRequest.weekCount/recurrent` (snapshot), crachá "Recorrente" + faixa no detalhe, `notifySupervisory` crítico (`src/lib/payments/recorrencia.ts`; refeito quando o aprovador muda o dia); **listas obedecem o seletor de unidade** do cabeçalho (`resolveUnitFilter`, `?unit=todas`, contagens idem); **v1.76.0**: **reprovar em lote** (`rejectManyRequests`, ação `rejectMany` em `/api/payments/batch` com motivo único; rota de lote passou para a aba Aprovar na matriz — estava em Pagar)) |
| 7.1 — Consolidado de freelancers da rede (v1.97.0) | ✅ Concluído (`/modulos/pagamentos/relatorio-freelancers` ganhou DUAS abas; `src/lib/payments/consolidado.ts` + `consolidado-tipos.ts`, `src/components/payments/consolidado-client.tsx`). **Aba Consolidado** = visão gerencial (quais unidades mais usam, quanto custa, onde há recorrência); **aba Fechamento (PIX)** = o documento do Financeiro, intacto. Filtros na URL (período esta semana/semana anterior/este mês/mês anterior/personalizado, unidade, tipo, status, recorrência), indicadores, comparação com o período anterior, evolução por semana (≤45 dias) ou por mês, seção "Freelancers recorrentes", tabela por unidade com % da rede e clique→filtra, detalhamento por freelancer em modal. **Nenhuma tabela nova e nenhuma regra nova**: tudo sai de `payment_requests` e a recorrência usa `semanaDe()` + `getFreelancerWeekLimit()` — os mesmos que disparam o alerta ao supervisor. ⚠️ **Duas decisões que não são óbvias:** (1) a recorrência é calculada, **não lida de `PaymentRequest.recurrent`** — o snapshot só existe desde a v1.75.0, então períodos anteriores apareceriam com zero recorrentes, em silêncio; (2) ela conta **semanas inteiras e sem o filtro de unidade** (um período que abre na quarta cortaria a semana ao meio; e "o freelancer é o mesmo em qualquer unidade"), mantendo sempre o **escopo do usuário** — a tela diz isso na própria seção. Status e recorrência filtram DEPOIS da conta da semana, senão o selo passaria a depender do que a pessoa escolheu ver. Rejeitadas ficam fora de `valorSolicitado` e aparecem numa linha à parte. **Dois defeitos achados no caminho**: o fechamento semanal tinha `/^d{4}-d{2}-d{2}$/` (`d` literal, sem a barra) — nunca casava, então "Ver semana" **nunca funcionou** e caía no mês sem erro; e as linhas da folha mostravam `createdAt` embora o período filtre por `workDate`, pondo data fora da semana anunciada. A Supervisão passou a ter o **link** para a consolidação (já podia entrar digitando o endereço) |
| 8 — Notas (QR + IA) | ✅ Concluído (leitura da chave 44 dígitos com pré-preenchimento, confirmação obrigatória, fallback manual/foto, status Recebida/Paga/Problema; **leitura por câmera de QR (NFC-e) E código de barras (Code-128 da DANFE)** — `QrScanner` usa BarcodeDetector nativo (formatos qr/code_128/code_39/itf/ean_13/codabar) + fallback `@zxing/browser`, `extractChave` cobre p=/chNFe=/run de 44 dígitos; IA Claude é ponto via env; **editar/excluir nota restrito a Supervisor/Admin/CEO** (v1.3.3): `canManageNotes` em `updateNote`+`deleteNote`, `DELETE /api/notes/[id]`, UI gate `canManage`; gerente lança e marca Paga/Problema; **status Devolvida** (v1.6.0): `NoteStatus.RETURNED` c/ motivo (reusa `problemNote`), botão "Devolver"; **aba Análise** (Supervisor/Admin) filtra fornecedor/unidade/status com totais; **reformulada (v1.18.0)**: lista por data desc c/ filtros completos, padrão 60d (`?dias=`), SEM botão Paga (Teknisa controla pagamento), fornecedor só da lista, Análise c/ campos completos+editar/excluir+`/api/notes/export`, `supervisorLaunched` desconta meta; **gás absorvido aqui (v1.41.0)**: fornecedor `isGas` → campos de gás no lançamento (cria `GasReceipt`) + vencimento; aba **Análise de gás** (embute `GasClient`, `basePath=/modulos/notas`); aba **Vencimentos** (`src/lib/notes/due.ts`, `/api/notes/due`) com filtros + alerta diário à supervisão+financeiro (`notifyUpcomingDueNotes`, `dueAlertedAt`)) |
| 9 — Pessoas (API RH) | 🟡 Integração RH ATIVA: sync por unidade (Unit.rhUnitName ↔ razão social) + "Sincronizar todas as cadastradas" — só entram colaboradores das unidades do SGO (outros segmentos do grupo ficam de fora). **Mapa de Funções 9.3** ✅ alocado no SGO; **cada alocação/remoção notifica os Admins** (avisar o RH). **Período de Experiência ≤90d** ✅ (v1.7.0, `ProbationReview`). **Avaliação do colaborador** ✅ (v1.8.0, item 13): observações do dia a dia + avaliação mensal 4 critérios 1–5★ c/ histórico 12m (`CollaboratorObservation`/`CollaboratorEvaluation`, `src/lib/people/evaluation.ts`, `/modulos/pessoas/avaliacao`); conta na meta como "Avaliações da equipe", peso `EVALUATION_META_WEIGHT` **padrão 0** (Admin liga na tela); missed só em mês encerrado; Admin exclui via /api/admin/ops. **Comissões & Mobilidade** ✅ (v1.10.0, item 14): `CollaboratorPayout` (COMMISSION|MOBILITY), lança Supervisor/Admin/CEO, dashboard mês/unidade/top10/tendência 12m (`src/lib/people/payouts.ts`, `/modulos/pessoas/comissoes`). **Férias — solicitar ao RH** ✅ (v1.10.0, item 11 provisório): status `REQUESTED`, form na aba Férias, anti-overlap, notifica Admins (`requestVacation` em people.ts). **Onda 3 concluída**; falta só a API real do RH p/ férias/mudanças/trocas. POPs com vídeo YouTube ✅ |
| — Central de Notificações | ✅ Concluído (in-app, sino com badge no header, /notificacoes, marcar lida; notifyAdmins/notifyRole/notifyUsers — usado pelo Mapa de Funções; demais módulos podem plugar). **Push no celular ✅ v1.38.0**: PWA instalável (manifest+ícones+`public/sw.js`) + **Web Push VAPID** acoplado ao `notifyUsers` (canal extra — o in-app é criado mesmo se o push falhar); ativação por aparelho em **Meu Perfil**, teste, lista de aparelhos e **preferências por categoria** (críticas ignoram); `src/lib/push/*`, `/api/push` (+`/key`), models `PushSubscription`/`PushPreference`; **inerte sem `VAPID_*` no .env** (gerar no servidor com `scripts/gen-vapid.mjs`) |
| 10 — POPs | ✅ Concluído (blocos texto/checklist/imagem/vídeo, publicação, confirmação de leitura por versão, versionamento; **criar/editar/excluir** com unidades+setores; **editor de blocos (v1.4.0)**: texto rico via contentEditable (negrito/itálico/listas/H2/link, sanitizado `sanitizePopHtml`), blocos reordenáveis por drag+▲▼, `.pop-rich` no globals; API aceita `blocks[]` com fallback legado texto+vídeos) |
| 10.1 — Treinamentos (POPs) | ✅ POP vira treinamento: **Inicial** (todo novo) e/ou **Setorial** (por nome de setor) + recorrência Único/Mensal. `src/lib/training.ts` reconcilia pendências (alocação no Mapa = setor; sync RH = novos; mensal; vencidos; re-treino por versão); prazos novo=7d/mensal=fim do mês. Aba `/modulos/treinamentos` por setor (cobertura 🟢🟡🔴, gerente marca "Treinei"). **Conta na meta**: componente "Treinamentos" peso único configurável (`AppSetting TRAINING_META_WEIGHT`, em getUnitMonthScore + getMetaBreakdown) |
| 11 — Metas | ✅ Concluído (ranking mensal, Minha Meta do Mês, detalhamento por tarefa/peso; **histórico de meses anteriores** (seletor 12 meses) + **relatório PDF (print) e Excel** `/api/metas/export`; **Configuração da Meta central** `/modulos/metas/config` (v1.23.0, Admin edita/Sup vê): checklists + componentes diários **Desperdício/Comandas por cobertura mensal** (dias preenchidos÷decorridos, `WASTE/COMMANDS_META_WEIGHT` default 0, `src/lib/metas/config.ts`) + Comunicados/Treinamentos/Avaliações + penalidade fora do prazo) |
| 12 — Auditoria | ✅ Concluído (tela com filtros por módulo, timeline imutável; **relatório PDF (print) + export CSV** `/api/audit/export` e `/auditoria/relatorio` por período 7/30/90d e módulo) |
| 13 — Configurações + LGPD | ✅ Concluído (cadastros Admin: Unidades, Usuários, Checklists (+**editar unidades** do checklist), **Comandas (multi-sequência)**, **Desperdícios (categorias)**, **Ocorrências (tipos/categorias)**, Pagamentos (freelancer c/ **PIX obrigatório**+valor padrão, tipos de avulso) via /api/admin com **editar/excluir** [exclusão bloqueada se houver histórico]; **Perfis de acesso** = matriz perfil×módulo Ver/Editar, oculta no menu o que o perfil não pode ver, ADMIN/CEO sempre totais; **submenus (v1.64.0)**: `MODULES[].parent` cria linhas recuadas para partes de dentro de um módulo (estreia com as 3 abas da Minha área — `MANAGER_AREA_TASKS/NOTES/LEAVES`), filho sem linha segue o pai e o pai é o teto; a rota `/api/manager-area` recusa a gravação da aba fechada (`src/lib/permissions/manager-area.ts` é a regra única de tela+servidor). **v1.65.0 — todo o sistema**: 45 partes com `nav` próprio (Tarefas, Comandas, Cancelamentos, Notas, Troco, Pessoas+**Escala**, Pagamentos, Metas, Ocorrências, relatórios e as 17 telas de Configurações) → a guarda de rota protege cada uma; filho com padrão próprio em `RESTRICTED_DEFAULT` vence a herança (telas de Configurações nascem Admin/CEO, exceto Usuários/Fornecedores/Catálogo=+Supervisão e Troco=+Coordenação); **buraco fechado**: `/modulos/escala*` não tinha dono e qualquer logado abria; atalhos somem junto (`src/lib/permissions/links.ts`); matriz abre dobrada com contador de partes fechadas; teste `permissions-rotas` varre o disco e quebra se uma tela nova ficar sem dono. **v1.66.0 — abas internas**: `src/lib/permissions/abas.ts` é a fonte única (a matriz é gerada dele em `MODULES = BASE.flatMap(...)`; tela e rota leem o mesmo registro) e cobre as 15 telas com aba (Pagamentos, Notas, Troco, Inventário, Manutenção, Supervisão, Produtos, Atestados, Comunicação, Desligamentos, Gás, Óleo, Pessoas, Escala, Ocorrências); `soVer` = aba de consulta (coluna Editar vira traço); 11 rotas recusam 403 via `permissions/guarda-abas.ts` (mapa ação→aba explícito, ação não mapeada não é barrada). **v1.67.0 — a matriz vale nas ROTAS**: `src/lib/permissions/guarda-rota-api.ts` (mapa rota→{módulo, exigir:'ver'|'editar'}, prefixo mais longo) + a mesma linha `guardaDaRota(user.role, req)` em 66 rotas; **71 das 105** conferem a matriz (era 1). Exportação exige só `ver` (senão leitor perde o relatório); rota não mapeada não é barrada; `FORA_DA_MATRIZ` documenta cada exceção (login, perfil, push, ficha pública, **QR de higiene é público**). `tests/api-guarda-cobertura.test.ts` varre `src/app/api` e exige que toda rota que grava tenha guarda. **v1.69.0 — auditoria das 16 "regra própria"**: 10 tinham regra real (motivo em `FORA_DA_MATRIZ` agora cita arquivo+função); 6 só olhavam `canAccessUnit` (CAIXA passava) e entraram em `REGRAS` — `regraDaRota` aceita **`[id]` como curinga de segmento**; `reclassify` de ocorrência só SUPERVISOR/ADMIN/CEO (rota + tela); `tests/api-guarda-fora-da-matriz.integration.test.ts`; aceite de termo, LGPD) |
| 13.1 — Perfis destravados + Setores do CD (v1.92.0) | ✅ Concluído. **Bug de fundo corrigido:** `ALL_ROLES` era escrito à mão e tinha 7 perfis enquanto o enum tinha 8 — o `SEPARATOR` ficou de fora, então `permissionMatrix` não o calculava e `setRolePermission` recusava toda célula dele como INVALID: **não havia como o Admin configurar o Separador**. Agora `ALL_ROLES = Object.values(Role)` (derivado do enum; esquecer deixou de ser possível) e o teste `perfis-e-setores-cd` trava a regressão. O seletor da tela de Perfis vinha de `EDITABLE_ROLES` com 4 nomes fixos no cliente → passa a vir do **servidor** (todos menos ADMIN/CEO = 6). `ROLE_OPTIONS` deixou de ser 2ª lista à mão: deriva de `ROLE_LABELS` + `ROLE_HINTS` (a linha de apoio usa o campo `hint` do Select, não parênteses no rótulo, porque o rótulo também vira crachá). **A tela em si não mudou** — mesma matriz, mesmos contadores "N partes"/"N fechada(s)", mesma trava de Editar-sem-Ver. **Setores do CD** (`/configuracoes/setores-cd`, `src/lib/products/setores.ts`, `/api/products/setores`): o módulo `CONFIG_CD_SECTORS` já existia na matriz apontando para uma tela que **nunca foi escrita** — daí o teste novo "todo módulo com endereço aponta para uma tela que existe", o caminho inverso que faltava. Setor em uso desativa, não exclui (FK `SetNull` apagaria de qual setor cada item saiu, em silêncio); desativar é barrado enquanto houver separador vinculado (a fila dele abriria vazia). **Cadastro do Separador**: campo Setor do CD obrigatório em Usuários, e o Separador **deixou de pedir unidades** (`listarParaSeparacao` só olha `cdSectorId` — o CD atende a rede toda). **Último Administrador ativo** não pode ser desativado, rebaixado nem excluído |
| 13.2 — Gestão de Perfis (v1.93.0) | ✅ Concluído (`/configuracoes/perfis` ganhou o bloco de gestão; `src/lib/perfis.ts`, `/api/perfis`, models `Profile` + `ProfilePermission`). O Admin **cria, renomeia, ativa/desativa e exclui** perfis, e configura cada um na matriz de sempre. **`baseRole` é o coração do desenho**: perfil aqui é duas coisas fundidas — o conjunto de TELAS (dado) e as REGRAS DE NEGÓCIO (**255 comparações `role === 'X'` em 146 arquivos** decidindo aprovador de pagamento, encerramento de ocorrência, `seesAllUnits`). Perfil sem base passaria por todas elas como `false`, **em silêncio**. Por isso o perfil novo declara de quem herda, e o `baseRole` é **replicado em `User.role`** — não só calculado na sessão — porque o sistema consulta o banco por perfil (`findMany({ where: { role: 'SUPERVISOR' } })` para notificar/listar aprovadores); só na sessão, essas consultas ficariam cegas. ADMIN/CEO **não servem de base** (`isFullAccess` curto-circuita a matriz: seria porta dos fundos com cara de configuração). Herança: parte do base JÁ CONFIGURADO e sobrepõe (`permissoesDoPerfil`), reusando `calcular()` — a mesma cascata pai→filho e a mesma trava Editar-sem-Ver dos perfis de sistema. **Tabela à parte** (`ProfilePermission`), sem tocar em `role_permissions`. **As guardas não mudaram de assinatura**: `permissoesDeQuemPergunta` resolve o perfil pela sessão, o que evitou reescrever as 66 rotas `guardaDaRota(user.role, req)` (e o risco de deixar uma para trás); a checagem `u.role === role` mantém honesto — perguntar "o que o CAIXA pode?" segue respondendo sobre o Caixa. `getSessionUser` passou a ser **memoizado por request** (`cache` do React, com guarda: no Vitest o `react` do cliente não o exporta). Perfil desativado → o usuário volta ao base. Excluir só sem usuários. ⚠️ **Armadilha encontrada no navegador**: as constantes do prefixo `perfil:` moravam em `admin-client.ts` (`'use client'`) e a página de servidor montava o `value` com elas — servidor recebe REFERÊNCIA, não o valor: id corrompido e "Dados inválidos" em toda gravação, sem erro de compilação. Moraram para `src/lib/perfil-valor.ts`, puro |
| — Pedidos: setor obrigatório e tela única (v1.94.0) | ✅ Concluído. **A página tinha DUAS telas de pedido**: a atual (`PedidoClient`, v1.86.0+) e uma herdada do módulo antigo (`NewOrder` em `products-client.tsx`, última mexida na v1.66.0). A antiga despejava os 1.184 produtos **e** gravava por `createProductRequests`: itens dentro do JSON legado, **sem `ProductRequestItem`**, **sem `cdSectorId`** e com status `NEW` — que `listarParaSeparacao` nem consulta. **Pedido feito por ela nascia invisível para o separador do CD**, sem erro nenhum. A aba "Novo pedido" passou a receber a tela atual por prop (`novoPedido`), o `NewOrder` saiu e `createProductRequests` foi **removida** (não desativada) junto com a ação `order` da rota — manter viva seria deixar a porta para recriar o problema por fora da tela. Abas Meus pedidos/Fábrica-CD e os pedidos já gravados: intactos. **`Product.cdSectorId` virou obrigatório para origem CD** em `upsertProduct` (motivo `SEM_SETOR`, com mensagem própria na rota); origem Fábrica **limpa** o setor, em vez de deixar dado órfão. Para os ~1.184 já cadastrados o conserto é EDITAR: o catálogo mostra em âmbar "N produto(s) do CD sem setor" com botão "Corrigir agora" (filtro) e o setor se escolhe **na própria linha**, salvando na hora — um modal por item inviabilizaria o mutirão. ⚠️ A **importação por Excel continua sem coluna de setor** (ela escreve direto no Prisma, não passa por `upsertProduct`): produto do CD importado nasce sem setor e cai no contador em âmbar. Atenção ao mexer nisso: em `sheet.ts` o rótulo "setor" já está mapeado para **categoria** |
| 14 — Escala (presença mensal) | ✅ Concluído (**v1.68.0**: cadastro da escala DENTRO do colaborador (folha em Pessoas→Colaboradores, lista mostra a vigente ou "Sem escala"), lista obedece o seletor de unidade + teto nomeado `LIMITE_DA_LISTA=500` com contagem; **Planejado congelável** — "Preencher automaticamente" (`src/lib/schedule/materializar.ts`) materializa o mês em `SchedulePlanOverride` (que já vencia o calculado e ninguém gravava) e registra autor/hora em `schedule_plan_fills`; refazer usa a config atual, Realizado intocado; **falta a fase 2** = aparência dos postos (barra de 3 botões, filtros, 4 blocos na grade). `/modulos/escala`: Planejado [gerado do padrão 12x36 par/ímpar, 6x1, 5x2, personalizada + ajuste], Realizado [única aba editável: T/F/FI/FJ/A/FE por célula, Registrar ausência por período c/ anexo PDF/foto, preencher auto, puxar=planejado], Comparação; export Excel/CSV + PDF via print). Lógica em `src/lib/schedule.ts`. **Trocas → RH** ✅ (v1.10.0, item 15): `ScheduleChange`, `/modulos/escala/trocas`, notifica Admins (`src/lib/schedule-changes.ts`). **Status ATRASO (AT)** ✅ v1.17.0: trabalhou atrasado — presente no Mapa do dia, gera aviso RH. **Avisos ao RH** ✅ v1.16.0: variações do Realizado → `RhScheduleNotice` + relatório `/modulos/escala/avisos-rh` |
| 14.1 — Escala de gerentes (v1.77.0) | ✅ Concluído (`/modulos/escala-gerentes`, `src/lib/manager-schedule-central.ts`, cartão em Pessoas). A agenda de quem responde pela unidade: filtros unidade/mês/ano e **grade de um gerente por linha × um dia por coluna** — `T` trabalha, `F` folga lançada, `FE` férias, `·` fora do padrão semanal, `?` sem horário. **Folga/férias lançadas vencem o padrão** (o contrário afirmaria cobertura inexistente); **dia sem gerente** em vermelho; quem não tem horário **não conta como cobertura** e unidade sem nenhum horário não sai toda vermelha. **Cadastro central** do horário semanal e do lançamento de folga/férias (fim em branco = 1 dia; período que encosta em outro do mesmo gerente = 409), o gerente é **notificado**, `manager_leaves.createdById` guarda **quem lançou** e tudo entra na Auditoria. **Quem lança**: módulo `MANAGER_SCHEDULE` restrito à **Supervisão** por padrão (+ADMIN/CEO) — `/api/manager-schedule` exige `Editar` **e** escopo por unidade em cada função. **Minha área → Folgas/férias virou consulta**: tela e rota passam pela mesma conta (`modulosDaOperacao`, lista de partes que precisam TODAS liberar); para devolver o lançamento ao gerente, ligue o "Editar" da Escala de gerentes no perfil dele) |
| 14.2 — Comissão e Mobilidade reestruturadas (v1.108.0) | ✅ Concluído (`src/lib/people/payouts-competencia.ts`, `payouts-export.ts` PURO, `components/people/payouts-competencia-client.tsx`, `/api/people/payouts` PATCH + `/export`). **DUAS ABAS independentes** na mesma tela — cada modalidade com a sua data de entrega, o seu fechamento e o seu arquivo. **Agrupado por UNIDADE** (padrão do SGO dos postos): a linha responde "quanto e quantos" e só quem precisa expande — com 279 lançamentos a lista corrida não responde nada. **Lote**: filtra unidade, busca por nome OU CPF, "aplicar aos N da lista" e ajuste linha a linha; campo vazio = não lança (é como se pula alguém sem desmarcar). **`PayoutDelivery`** = uma data por unidade+competência+tipo, herdada pelos lançamentos (a entrega é do LOTE; pedi-la por linha multiplicaria por 50 uma informação que é uma só); data vazia **apaga** o registro, senão uma data errada mandaria "entregue" no arquivo. **`PayoutClosure`** trava lançar/editar/excluir/entrega **daquela** modalidade (fechar comissão não trava mobilidade — elas vão para a administradora em arquivos e momentos diferentes); exportar continua liberado; só ADMIN reabre, e a reabertura vai para a Auditoria porque "fechado" é ESTADO, não histórico. ⚠️ **DOIS Excel separados e NÃO existe "exportar tudo"**: a rota não sabe montar arquivo misto (tipo obrigatório, entra no filtro, no nome do arquivo e no da aba) — a forma segura de garantir a separação é não oferecer o caminho. O formato saiu do **arquivo REAL** da administradora (12 colunas), inclusive a coluna `Unidade` **vazia** ao lado de `Unidade Trabalho` preenchida: é assim que ela aceita hoje, e preencher mudaria um formato que funciona. ⚠️ **`Novato` conta até o FIM DA COMPETÊNCIA, nunca até hoje** (90 dias, decisão do Pedro) — contra "hoje", o arquivo de janeiro diria uma coisa em fevereiro e outra em junho, e arquivo já enviado não pode mudar de resposta ao ser reemitido; há teste. **O que o SGO não sabe fica VAZIO**: `No Prazo` exigiria prazo acordado que não existe ("Sim" sem comparação pareceria conferido), `Status` só vira "entregue" com entrega registrada. `ID` é **sequencial da exportação**, não o cuid. **CPF e admissão saem do CADASTRO na leitura**, não copiados para o lançamento — correção no RH aparece no próximo arquivo sem reescrever histórico; o NOME segue congelado, que é o que mantém legível quem já saiu. A tela **nomeia as unidades sem lançamento** (sem isso, unidade esquecida só aparece quando a administradora reclama). ⚠️ **A exportação CSV anterior MISTURAVA as duas** (coluna "Tipo") e foi substituída; rota mudou de `?year=&month=` para `?tipo=&mes=`. Cliente antigo **removido**, não deixado ao lado (duas telas para a mesma coisa foi o defeito da v1.94.0). ⚠️ **API do RH conferida**: ela entrega `matricula/nome/cpf/status/unidade/cargo/admissao` — `Data Entrega`, `Status` e `No Prazo` **não são dado de RH** (o RH não sabe o dia em que a unidade entregou), e só `Novato` é derivável. Existem 3 endpoints financeiros nunca ligados (`/financeiro/vale-transporte`, `/beneficios`, `/folha`) que podem mudar isso — a `RH_API_KEY` está vazia em dev e não deu para sondar. Migração aditiva (2 tabelas), 49 testes; o arquivo foi gerado e lido de volta contra o modelo real |
| 9.3 — Mapa de Funções (turnos) | ✅ Turnos por unidade (Admin CRUD); setores **editar/excluir** + ordem alfabética + setores de referência. **Reformulado (2026-06-26):** **quadro padrão em 2 listas** ("A alocar" × "Alocados" editável, `getAllocationBoard`/`updateAllocation`); **mapa derivado da Escala em tempo real** (`getUnitDayMap`: só quem tem status WORK no dia E dentro da janela do turno — turno sem horário = dia todo; folga/falta/atestado/férias somem do dia automaticamente) + **seletor de dia**; **freelancers na alocação** (pedido de pagamento ganha hora início/fim `workStartTime/workEndTime` → painel "Freelancers do dia" aloca em setor via `workSectorId`, `getDayFreelancers`/`assignFreelancerSector`); **histórico por dia** congelado (`WorkforceDaySnapshot`, scheduler `snapshotYesterdayAllUnits` 1×/dia, leitura `getSnapshotGrid` — passado imutável). Admin pode **excluir lançamentos** de toda a Operação (reversão + auditoria). **Mudanças de função/setor → RH** ✅ (v1.9.0, item 12): editar alocado ganha campo **Função (cargo)**; setor muda no SGO + registro, função vira **solicitação ao RH** (sync sobrescreve jobTitle — não edita local); tudo notifica Admins e entra no registro `RoleChange` em `/modulos/pessoas/mudancas` (`src/lib/people/role-change.ts`); **simulação salvável em dia futuro** (v1.23.0, `WorkforceSimulation` unitId+date, rascunho de alocação sem alterar o quadro padrão) |
| — Mapa: "Necessário 24 horas" vira escolha (v1.101.2) | ✅ Concluído. 🔴 **Em produção**: toda função aparecia exigindo cobertura 24 horas, e cadastrar `06:40–15:00` era recusado por conflito com `00:00–24:00`, sem saída pela tela. **A causa NÃO era o cálculo** — faixas que encostam já eram aceitas (`[início, fim)`: 06:40–15:00 e 15:00–23:40 convivem), a que cruza a meia-noite já funcionava e setor sem faixa já ficava *sem exigência*; e não havia preenchimento automático rodando. Era a **migração da v1.84.0**, que ao substituir `Sector.minHeadcount` converteu cada setor numa faixa `00:00–24:00` "para não mudar nada no dia da subida": a faixa de dia inteiro ocupa os 1440 minutos, então QUALQUER horário específico colide, e a tela de adicionar não oferecia volta. **Agora "Necessário 24 horas" é caixa de marcar** (`definirVinteQuatroHoras` em `cobertura.ts`, ação `24h` na rota): marcar **substitui** as faixas do setor (pedir 24h e manter uma faixa das 10h às 15h seria pedir duas coisas, e a segunda ficaria inerte, porque a de dia inteiro cobre o minuto primeiro); desmarcar apaga só a faixa de dia inteiro e deixa o setor **sem exigência** — "não é 24 horas" não diz qual é o horário, e inventar um seria pior que não ter nenhum (setor sem faixa não gera alerta). A recusa por conflito com a faixa de dia inteiro passou a **apontar a saída** ("desmarque Necessário 24 horas") em vez de "conflito de horário", que mandava procurar uma sobreposição invisível. Faixas ganharam **editar** (antes só apagar e recadastrar) e a dica diz os dois casos que parecem erro e não são: encostar e atravessar a meia-noite. **No painel**, funções sem necessidade naquele horário ficam **escondidas** com botão `Mostrar funções fora do horário (N)` — função sem faixa às 2h não está descoberta, e mostrá-la junto das que importam ensina a varrer a tela; o rodapé do cartão virou "Sem necessidade neste horário". O miolo do acordeão virou `CorpoDaNecessidade` exportado: o acordeão nasce fechado e `renderToString` não clica, então sem separar nada do formulário seria verificável — e o teste não deve depender de prop que só existe para ele. ⚠️ **Nada foi apagado do banco**: os `00:00–24:00` herdados continuam lá porque alguns são legítimos (Caixa e Balcão de unidade 24 horas); quem precisa de horário específico desmarca a caixa naquele setor, uma vez por função. 22 testes novos, incluindo o cenário do pedido (Caixa 24h + Cozinha em dois turnos + Churrasqueira com buraco entre 15h e 18h) e a reprodução do beco |
| — Acabamento visual (v1.102.0) | ✅ Concluído. Modernização **de acabamento**, com o SGO dos postos como referência de acabamento e **não de paleta** — nenhuma tela mudou de lugar, nenhum nome de aba mudou, nenhuma rota, permissão ou regra mudou. ⚠️ **A identidade bordô virou teste** (`tests/identidade-visual.test.ts`): o risco é silencioso porque quase todo painel de mercado é azul, `info` (azul) **é** token válido do sistema e trocar a marca por ele não quebraria o lint nem o guard de paleta — que só confere se a chave existe, não qual chave carrega a marca. Os casos travam o RGB do bordô, exigem R > B em todas as tintas, e que o ativo do menu e o chip padrão do cartão usem `brand`. **Elevação virou TOKEN**: `--sgo-shadow-card` (repouso), `-card-hover` e `-pop` (mega menu), as três discretas de propósito — o cartão já se separa do canvas pela borda, e sombra forte num painel de dez cartões é sujeira e não modernidade; **zeram no `@media print`**, porque no papel sombra vira borrão cinza (o bloco de impressão já existia desde a v1.100.1). **Menu superior**: o ativo virou **pílula de tinta bordô** no lugar do sublinhado de 2px colado na borda da barra (era o risco embaixo que dava cara de barra de sistema antigo); hover na mesma tinta mais clara, para o destino do ponteiro ficar óbvio sem introduzir um cinza que não existe no sistema; o mega menu trocou `shadow-lg` do Tailwind pela sombra do design system. **Cartões do Dashboard**: ganharam **chip de ícone** (`Indicador.icone` é CHAVE — `central.ts` é módulo de servidor e não carrega JSX; a tela resolve para o componente, como `ICONES` da TopNav), `p-3`→`p-4`, e mudança de sombra no hover, que diz "clicável" sem gastar mais uma cor; o chip segue o tom do cartão e **o padrão é a marca** — vermelho e âmbar ficam para o que está de fato fora do lugar. **"Precisa da sua atenção"**: o rótulo subiu para a primeira linha e virou **crachá** — embaixo, em caixa alta e cinza, ele tinha peso de legenda mas ocupava o lugar da informação, e a segunda linha ficava com três coisas concorrendo; **nada foi removido** (quando, ação e unidade seguem lá). **StatusBadge** ganhou anel interno de 1px na própria cor: sobre `surface` os fundos de status são claríssimos (danger-bg é 254 243 242) e a pílula sumia, virando texto colorido solto. `Card` passou de `rounded-xl`/`shadow-sm` do Tailwind para `rounded-card`/`shadow-sgo-card`, para o acabamento seguir o token e sumir sozinho na impressão. Conferido no navegador em 1280px e 375px, sem transbordo |
| — RH auto-sync | ✅ Sincronização automática ~1×/dia via scheduler (`instrumentation.ts`, log RH_SYNC_AUTO), além do botão manual |
| — API do RH v2: transporte + diagnóstico (v1.112.0) | 🟡 **Em preparação — o sync SEGUE na v1.** O RH publicou `https://gbf-rh.replit.app/api/ext/v2/rh` com chave própria (`RH_API_V2_URL`/`RH_API_V2_KEY`, no `.env` do droplet e local; **nunca no Git**). `src/lib/rh/client.ts` ganhou `rhGetV2`/`rhV2.raw(path)` sobre o MESMO núcleo de transporte da v1 (`doGet`: `x-api-key`, timeout 20s); `src/lib/rh/v2-ping.ts` (`resumirForma` PURA = só nomes de campos, nunca valores); rota **`/api/rh/v2/ping` (Admin/CEO, `CONFIG_INTEGRATIONS`/ver) segura para PRODUÇÃO** + botão "Testar conexão v2" na central de Integrações; `/api/rh/test?v=2&path=…` (só dev) despeja o JSON cru. ⚠️ **Achado em 23/09/2026**: a chave v2 é VÁLIDA na v1 (200 em `/api/ext/colaboradores/unidades`) mas a v2 devolve **401 "Não autorizado"** para tudo, em todas as formas de enviar a chave; a resposta traz `Vary: x-api-key`, então o header está certo e é a chave que não está liberada para a v2 — ou há **lista de IPs** (só o droplet). Não deu para testar do droplet: a chave SSH `bjf_vps` não está nesta máquina; por isso o ping vive em produção. **Pendências**: documentação da v2 (endpoints e formato) e liberação da chave; só depois `normalize`/`sync` migram, com a v1 de fallback até validar numa unidade real. `rhV2` não tem endpoints nomeados de propósito (nomear antes de ver a resposta seria chutar) |
| 19 — Central de Atestados | ✅ Concluído (lançar atestado por **foto com leitura por IA** — `src/lib/ai/atestado.ts`, inerte sem chave — que pré-preenche o form e destaca campos de baixa confiança; **colaborador via picker do RH**; ao salvar **marca os dias como "Atestado" na Escala** (`registerAbsence`, exceto tipo HORAS) e **notifica o RH**; tipos dia/horas/acompanhamento; **CID = dado sensível LGPD, visível só a ADMIN/CEO**, mascarado p/ demais e omitido no PDF; **painel** por unidade — dias perdidos, ranking, **taxa de absenteísmo** (dias÷headcount×dias do mês), tendência 12m, por dia da semana, por tipo; **relatório PDF (print) + export CSV** `/api/certificates/export`; anti-duplicidade colaborador+período; Admin exclui via `/api/admin/ops` (reverte a Escala); `src/lib/certificates/*`, `/modulos/atestados`) |
| 20 — Área do Gerente (v1.3.0) | ✅ Concluído (`/minha-area`, **todos os usuários**: **tarefas pessoais** com lembrete por notificação na data/hora — scheduler `notifyDueManagerTasks`, **editar/excluir + horário 30min** (v1.5.0); **bloco de notas** com **título+edição+texto rico** (`ManagerNote.title`, HTML sanitizado via `sanitizePopHtml`, componente `RichText` compartilhado) (v1.5.0); **folgas/férias** — nesses dias os checklists somem da aba Tarefas do gerente, `leaveOnDate`; **desde a v1.77.0 esta aba é CONSULTA** — quem lança é a Supervisão, no módulo Escala de gerentes; models `ManagerTask/ManagerNote/ManagerLeave`, `src/lib/manager-area.ts`, `/api/manager-area`). **Meu Perfil** ✅ v1.17.0 (`/perfil`, avatar no header): nome+CPF+troca de senha própria (`src/lib/auth/profile.ts`); usuários read-only p/ Supervisor/CEO. **Mobile**: `/modulos` espelha a sidebar c/ permissões (v1.17.0) |
| 21 — Desligamentos (v1.3.1) | ✅ Concluído (gerente solicita → **supervisor aprova/recusa** → RH; puxa **tempo de empresa** de `Collaborator.hireDate` [RH sync grava `admissao`] + **atestados/dias** do sistema; idade manual [RH não dá nascimento]; tipo de aviso trabalhado/indenizado; **relatório PDF**; `Termination`, `src/lib/terminations.ts`, `/modulos/desligamentos` (card em Pessoas)) |
| 22 — Padrão de produtos por foto (v1.3.2) | ✅ Concluído (catálogo `ProductStandard` com foto de referência em **Configurações → Padrão de produtos**; no checklist, itens `aiCheck` ganham botão **"Conferir padrão de produtos (IA)"** que compara a foto da vitrine com o padrão e lista itens fora do padrão — `src/lib/ai/product-standard.ts`, `/api/ai/product-standard-check`; inerte sem chave) |
| — Fornecedores (cadastro compartilhado) | ✅ Lista única `Supplier` (nome, CNPJ, PIX, categoria) gerida por **Admin/CEO/Supervisão** (`/api/suppliers`, `/configuracoes/fornecedores`); usada por **Gás, Notas Recebidas e Pagamentos** (`supplierId` opcional em `ReceivedNote`/`PaymentRequest`, picker que auto-preenche) |
| 17 — Coleta de Óleo | ✅ Concluído (lançamento por unidade: litros, valor/litro, **valor total a receber**, forma de recebimento PIX/dinheiro/crédito/troca, empresa coletora; dashboard litros+R$ por unidade e por forma + tendência mensal; histórico; Admin exclui via /api/admin/ops; `src/lib/oil/*`, `/modulos/oleo`). **v1.96.0 — comprovante e conferência**: a coleta é a única movimentação em que sai mercadoria e entra dinheiro por fora do caixa, então **`receiptPath` virou obrigatório** no lançamento novo (`SEM_COMPROVANTE`) e a rota passou a `multipart/form-data` (`saveEvidence`, só imagem). A coluna é **nullable de propósito**: as coletas antigas não serão reescritas — a tela as marca "Sem foto" e o cartão de conferência explica, em vez de exibir um traço que pareceria dado faltando por engano. **Quem coletou** também virou obrigatório (fornecedor do cadastro **ou** `collectorName` digitado — `SEM_COLETOR`; o campo já existia no banco e a tela nem o coletava). **Data da coleta** ganhou campo (antes gravava sempre hoje): retroativa sim, futura não (`DATA_FUTURA` — coleta que não aconteceu não tem recibo). **Responsável pelo lançamento** = `createdById` da SESSÃO, nunca do corpo, exibido como texto e não como campo. **Conferência** = clicar a linha do histórico abre o lançado e o recibo **lado a lado** (`Modal` size lg; no celular empilha), com "Ampliar" em tela cheia reusando `useDialogBehavior`/`useBodyPortal` sem a moldura do Modal. **Histórico**: o período (30/90/180/365/tudo) corta **no banco** via `?dias=` — o teto de 150 linhas saiu, porque filtrar por responsável dentro de um bolo já cortado responderia "nenhuma coleta" a quem tem coletas fora do teto. A coluna Unidade só aparece para quem enxerga mais de uma. ⚠️ A foto é gravada **antes** de a regra decidir (sem ela `createOilCollection` nem avalia o resto), então a rota chama **`removeUpload`** quando o lançamento é recusado — senão data futura/litros zero deixavam órfão no volume (encontrado testando no navegador, não pelas verificações) |
| 16 — Recebimento de Gás | ✅ Concluído (lançamento com leitura QR/código de barras + fornecedor; **quantidade em kg + valor total → preço real/kg**; **alerta de variação** vs última compra da unidade, limite `AppSetting GAS_ALERT_PCT` padrão 10% → notifica gerente+supervisor; **dashboard** comparando preço médio/kg **por unidade e por fornecedor** + tendência mensal; histórico com badge de variação; Admin exclui via /api/admin/ops; `src/lib/gas/*`, `/modulos/gas`; **contratos por unidade+fornecedor** (`GasContract`: período, kg contratado, preço/kg; baixa automática via recebimentos, % cumprido, posição atual) + **filtros unidade/fornecedor/mês** no dashboard e histórico (v1.21.0)) |
| — Gás: variação recalculada + relatório por unidade (v1.103.0) | ✅ Concluído (`src/lib/gas/variacao.ts` puro, `getRelatorioDeGas`/`getVariacoesPorNota` em `query.ts`, `/modulos/gas/relatorio` refeita, `/api/gas/export`). 🔴 **Dois defeitos em produção, os dois silenciosos**: a variação era calculada UMA VEZ no lançamento e gravada em `prevPricePerKg`/`variationPct`. (1) **Nota retroativa comparava com o futuro** — a referência era "a nota mais recente da unidade" (`orderBy operationalDate desc`), não a ANTERIOR à que entrava: lançar hoje uma compra de junho comparava junho com julho, e as notas de julho seguiam apontando para maio, como se junho não existisse. (2) **Correção de data não recalculava nada** — `editEntryDate` troca o `operationalDate` e mais nada; era o caso do print, com a nota "Data corrigida por Marcelo Teles" sem variação nenhuma e as vizinhas comparando com quem já não era vizinho. **A saída foi PARAR DE GUARDAR**: variação é função da SÉRIE ORDENADA e quem pergunta calcula, então a correção de data se resolve sozinha — não há gatilho para alguém esquecer de chamar, que é como os dois defeitos nasceram. A ordem é data e, no mesmo dia, **ordem de lançamento** (no arquivo real há QUATRO notas no mesmo 23/07; sem desempate estável a variação mudaria entre duas leituras da mesma tela). `alerted` **continua gravado de propósito**: não é "a variação desta nota", é o FATO de um alerta ter sido disparado naquele dia — recalcular um fato do passado seria reescrever história. Nota sem kg não entra na cadeia e **não a interrompe** (apagar o elo faria a seguinte comparar com uma terceira lá atrás, inventando um salto). **Relatório por unidade** no mesmo endereço, com filtro de período: notas, kg, valor, preço médio/kg, menor, maior, último e variação do período; clicar na unidade abre todas as notas (por `?unidade=`, sem estado no cliente — o link que o supervisor manda já abre certo e a folha impressa sai igual à tela); consolidado da rede no rodapé; Excel (resumo + detalhamento no MESMO arquivo, porque dois arquivos obrigariam a abrir os dois para responder "por que a média subiu") e PDF pela impressão. ⚠️ **O preço médio/kg é PONDERADO** (valor ÷ kg), não a média dos preços — mesmo erro do ticket médio consolidado, e igualmente plausível: a média simples dá o mesmo peso a 30 kg e a 600 kg; a tela diz isso em texto. **ÂNCORA**: o relatório busca também a última nota ANTERIOR ao período, para a primeira linha ter contra o quê comparar — sem ela a variação de uma nota mudaria conforme o filtro escolhido, e dois relatórios da mesma nota diriam números diferentes (há teste exatamente para isso). ⚠️ Achado no caminho: a tela **quebrava** com período sem data final — string vazia no `PeriodPicker` estourava no `DatePicker` (`parseISO('')`) e derrubava a página; período aberto agora vira HOJE. **Nada alterado ou excluído no banco**: as colunas gravadas continuam lá e alimentam o alerta do momento; o que mudou é que a exibição deixou de lê-las (a aba de gás dentro de Notas também passou a usar `getVariacoesPorNota`). 28 testes |
| — Gás: filtro de variação por unidade e período com PDF/Excel (v1.113.0) | ✅ Concluído (`src/lib/gas/periodo.ts` PURO com os presets, `VariacaoPorUnidadeCard` em `gas-client.tsx`, `AutoPrint` em `components/shared`). O cartão no Dashboard da Análise de gás **não calcula nada**: monta a URL de `/modulos/gas/relatorio` (que já recalcula a variação da série) com `start`/`end`/`unit`; PDF = a própria folha do relatório com `?imprimir=1` (abre no diálogo de impressão em nova aba); Excel = `/api/gas/export` que passou a aceitar `unit`. Na página do relatório: `unit` FILTRA (e já abre a unidade detalhada nota a nota) enquanto `unidade` só ABRE o detalhe, como antes; seletor de unidade no topo (`UnitSelectNav` com "Todas"); `PeriodPicker` ganhou `extra` para manter a unidade ao trocar o período; o link de volta aponta para `/modulos/notas/gas` (onde a análise vive desde a v1.41.0). "Últimos N meses" começa no dia 1 de N−1 meses atrás — é como as pessoas leem, não uma janela de 90 dias que corta um mês no meio (teste) |
| — Gás: as colunas voltam a subir (v1.104.0) | ✅ Concluído (`precoImplausivel`/`mediaPonderada`/`somarNotas` em `gas/variacao.ts`, `getGasMaxPriceKg`/`setGasMaxPriceKg`, `alturaDaBarra` em `gas-client.tsx`). 🔴 **Relato: "as colunas não estão subindo"** — era do gráfico, mas a causa estava nos DADOS e atingia a tela inteira. Os três números do print fechavam a conta: preço médio R$ 48,9914/kg ao lado de último preço R$ 7,0162/kg, e R$ 2.203.879 para 46 toneladas (o real seria ~R$ 300 mil) = **uma nota de ~R$ 1,9 milhão com ~300 kg**, a assinatura do TOTAL da nota lançado na coluna do preço unitário. Como **todo gráfico de gás é escalado pela série**, essa nota sozinha jogava as colunas de todos os meses e as barras de todas as unidades no piso de 4px. **A porta era a importação por planilha**: o modelo diz "Preço unitário = preço POR KG", nada verificava, e ali o erro é pior porque o import ainda **MULTIPLICA** pela quantidade. **Faixa de plausibilidade** (teto `GAS_MAX_PRICE_KG`, padrão R$ 30,00/kg, Admin ajusta no painel) barra o lançamento, a **correção** e a linha da prévia do import — o teto não julga se a compra foi cara, separa preço de **QUILO** de preço de **OUTRA COISA** (R$ 293 é o botijão P45 inteiro; milhares é o total da nota), e a recusa **diz o que conferir**, porque "inválido" faria reimportar igual. As notas já gravadas saem **NOMEADAS** em aviso no topo (unidade, data, preço, kg, valor) com o caminho do conserto — antes a tela só ficava estranha sem dizer por quê; **nada é alterado ou excluído no banco**. ⚠️ Dois defeitos independentes vieram junto: (1) o **preço médio era média SIMPLES** dos preços (mesmo erro do ticket médio consolidado — dava peso igual a 30 kg e a 600 kg), agora **ponderado** no cartão, por unidade, por fornecedor e no mês da tendência; (2) as barras iam **de ZERO** ao máximo, e como o gás varia de 6,46 a 7,02 a menor coluna já tinha **92%** da altura da maior — o gráfico que existe para mostrar variação era o que a escondia; a escala passou a ir do menor ao maior com 20% de folga, o que também tira de uma nota fora de faixa o poder de achatar as outras (série estável cai em meia altura: não há variação a mostrar). E o **"último preço/kg" dependia da ordem do banco** — `date >=` sem `createdAt` no `ORDER BY`, com QUATRO notas no mesmo 23/07 no arquivo real; o desempate entrou na consulta e a agregação passou a usar `ordemCronologica`. 27 testes novos; o caso do preço ponderado foi verificado **reprovando** o código anterior, e o do desempate está escrito para fixar o contrato (a falha é não-determinística por natureza, e o comentário do teste diz isso) |
| 23 — Rotina do Supervisor (v1.12.0) | ✅ Concluído (`/modulos/supervisao`, módulo `SUPERVISION` default restrito a SUPERVISOR; `src/lib/supervisor/*`). **Fase A** Painel de uso por unidade/mês (checklist%, cobertura desperdício/comandas = dias lançados ÷ decorridos, ocorrências, notas, caixas, meta; 🟢≥80/🟡≥50/🔴; piores primeiro). **Fase B** Visitas: `SupervisorVisit` (agendar → notifica gerente; concluir c/ feedback obrigatório → notifica gerente; cancelar; atrasadas destacadas; números do mês). **Fase C** Checklists de visita: `SupervisorChecklist` (CRUD Admin `/configuracoes/checklists-supervisor`, itens JSON; preenchido na conclusão da visita OK/Não+obs, resultado congelado em `checklistResults`; excluir com histórico = inativa). Admin exclui visitas via /api/admin/ops. **v1.13.0**: **recorrência de visitas** (`SupervisorVisitPlan` 1/unidade "a cada N dias", concluir reagenda, vencida avisa 1×/dia via scheduler) + **resumo semanal de aderência** (`runWeeklyAdherenceDigest`, AppSetting `ADHERENCE_DIGEST_LAST`, cobra supervisor/consolida p/ admins) + **export CSV** `/api/supervision/export` |
| 24 — Visão Executiva (v1.14.0) | ✅ Concluído (`/modulos/executivo`, `src/lib/executive.ts`, módulo `EXECUTIVE` default só CEO/Admin). A rede em 1 tela/mês: cartões (meta média, uso, desperdício kg, dias atestado, divergências troco, custo manutenção, ocorrências graves HIGH/CRITICAL, visitas) + tabela por unidade ordenada pela meta c/ semáforo de uso; Imprimir/PDF. Composição pura dos módulos existentes (mesmos números das telas de origem) |
| 18 — Gestão de Troco | ✅ **Reformulado como COFRE (v1.22.0)**: `CashVault`/`CashBucket`/`CashVaultMovement` (`src/lib/cash-vault.ts`, contagem por denominação 200→0,05 + "outros"); baldes c/ metas fixadas pela Supervisão; contagem diária substitui saldos; reposição de moedas e troca 1:1 c/ escritório (validada, tolerância 0,011); **retirada p/ pagamento = PROIBIDA** (fluxo vermelho c/ confirmação, alerta crítico supervisor+admins, contador/mês no Executivo); indicador notas grandes ≥50%; modelo antigo `CashSession` preservado (dados intactos, tela substituída) |
| 25 — Controle de Pizzas (v1.91.0) | ✅ Concluído (`/modulos/pizzas`, `src/lib/pizzas/*`). **Exclusivo da unidade com pizzaria** — e este é o PRIMEIRO módulo com recorte por UNIDADE: a matriz de perfis não tem essa dimensão (`viewableNavHrefs`/`canOpenPath`/`guardaDaRota` recebem só `role`), então o corte mora em `Unit.hasPizzeria` e é aplicado nos DOIS lugares — `recortarPizzas` subtrai o item de `viewable` (sidebar em `(app)/layout.tsx` e hub em `modulos/page.tsx`) e a página faz `notFound()` se `unidadesComPizzaria` vier vazia. Marcador de cadastro, **não o nome da unidade fixo no código**: corrigir a grafia de "Jardim Teresópolis" não derruba o módulo e uma 2ª pizzaria não exige deploy. **Preenchimento por LINK PÚBLICO sem login** (`/pizzas/[token]`, `/api/pizzas` em `PUBLIC_PREFIXES` + `FORA_DA_MATRIZ`): a unidade sai do token (`unidadePorToken`), nunca do corpo — quem abre não escolhe nem troca de unidade; sem QR. Fechamento = 1 por unidade/**dia operacional** (`@@unique([unitId, operationalDate])`) com N linhas tamanho×sabor; **tamanho é enum** (`PizzaSize` CM35/CM30/CM25) e **sabor é tabela** (`PizzaFlavor` por unidade, `flavorName` congelado no item) preparando o cruzamento futuro com ficha técnica — texto livre não casaria com receita; **baixa de estoque NÃO implementada** (validar quantidade/tamanho/sabor primeiro). Total ao vivo antes do envio (`totalDePizzas`, o mesmo cálculo do painel). **Duplicidade não sobrescreve calado**: 2º envio na mesma data → 409 "Já existe um fechamento para esta data."; a tela oferece "Carregar e corrigir" → `substituir: true`, que avisa o gerente e grava `PIZZA_CLOSING_UPDATE` na Auditoria. Retroativo limitado a 30 dias (link sem login) e futuro nunca. Painel: hoje, total, média **por dia lançado** (dia sem fechamento é dado que falta, não venda zero), tamanhos e sabores mais vendidos, histórico diário, filtro 7/30/90d. **Ativação sem deploy**: chave "Esta unidade tem pizzaria" em Configurações → Unidades (`updateUnit`, campo `hasPizzeria`; selo "Com pizzaria" só na unidade ligada) e catálogo em **Configurações → Pizzas (sabores)** (`CONFIG_PIZZAS`, `src/lib/pizzas/catalogo.ts`, `/api/pizzas/sabores` — autenticada, sob o prefixo público como `/api/higiene/manage`; sabor **já lançado não se exclui, desativa**, senão a FK levaria as linhas de fechamento junto; renomear não reescreve o histórico)) |
| — Pedido dividido por destino + impressão no tema escuro (v1.95.0) | ✅ Concluído. **Carrinho misto virava UM pedido só**: `criarPedido` carimbava o pedido inteiro com `input.origin ?? produtos[0].origin` — a origem do PRIMEIRO produto. Um carrinho com pão da Fábrica e arroz do CD virava um pedido `FABRICA` nascido em `ENVIADO_CD`, com os dois lados vendo item que não era deles. Agora os itens são agrupados por `product.origin` e sai **um pedido por destino**, cada um com número próprio (o retry do sequencial roda por pedido) e o status da SUA esteira — `NEW` para a Fábrica (fluxo da aba Fábrica/CD), `ENVIADO_CD` para o CD. A divisão por setor, que é a de DENTRO, não mudou. `criarPedido` retorna `pedidos[]` em vez de `{id, number}` (o parâmetro `origin` saiu: era ignorado e mentiria). Notificação certa para cada lado: separadores dos setores atingidos ← pedido do CD; `notifyAdmins` ← pedido da Fábrica. Na tela, `destinos` calcula o rótulo da revisão pela origem do que está no carrinho e a confirmação lista os dois números. **Impressão em branco**: o tema escuro repintava `--sgo-ink-900` em quase-branco e `print:bg-white` forçava o papel branco = **texto branco em papel branco**, em TODO relatório (só o romaneio escapava, por carregar `.sgo-print`). Os dois blocos de dark em `sgo-design-system.css` passaram a viver dentro de `@media screen` — na impressão a página cai sozinha nos valores claros de `:root`, sem repetir a paleta num segundo lugar |
| — Pedido por embalagem + contrato de gás que se explica (v1.109.0) | ✅ Concluído. **PEDIDO**: depois de bipar/buscar, uma **tira de 4 botões** (Unidade·Fardo·Display·Caixa) na própria linha do item + quantidade — "Coca-Cola 350ml — 2 fardos" (`embalagem-pedido.ts` PURO, enum `OrderPackUnit`, `ProductRequestItem.packUnit`). ⚠️ **NÃO converte e NÃO consulta o cadastro** para saber quantas vêm dentro: é registro do pedido, e quem separa lê "2 fardos" e separa 2 fardos (teste trava com `rotuloDaQuantidade.length === 3` — nenhum parâmetro é o fator). **Enum PRÓPRIO, e não o `PackType` do Estoque**: lá é propriedade do PRODUTO e a conversão é obrigatória; aqui é o que o gerente quer AGORA e a conversão é proibida — enums separados tornam a distinção estrutural em vez de convenção que alguém quebra em seis meses. Tira **na linha**, 1 toque: suspenso/modal custaria 2 toques por item e o gerente repete 30 vezes. "2 fardos" atravessa até o separador, o recebimento e o romaneio (teste de integração). Sem escolha → `UN` + medida do cadastro (comportamento de sempre); valor inválido do corpo também cai em `UN`. **CONTRATO DE GÁS**: relato de 39.385,6 contra 39.814,6 somados — diferença de uma nota de 429 kg (08/09/2023). ⚠️ **Não era o "Data corrigida"** (o cálculo nunca olhou `dateEdited`; outras notas com o mesmo aviso entraram). A causa da CLASSE está no modelo: **não há FK entre `GasReceipt` e `GasContract`** — o vínculo é INFERIDO por unidade + fornecedor + janela, e qualquer uma das três exclui **em silêncio**. Enquanto não há FK, o contrato passou a **listar o que deixou de fora e por quê** (fora do período · outro fornecedor · sem fornecedor), cada motivo apontando um conserto diferente; nota fora do período E de outro fornecedor **não** entra no aviso (não diz nada sobre este contrato, e listá-la seria ruído). O cálculo virou **uma consulta só** para todos os contratos em vez de um `aggregate` por contrato; a regra de soma **não mudou**. Migração aditiva (1 enum, 1 coluna), 27 testes |
| — Navegação por áreas + busca global + Toda a Rede (v1.98.0) | ✅ Concluído (etapa 1 da repaginação). **Havia QUATRO listas de módulos escritas à mão** — `NAV_GROUPS` (sidebar), `FAMILIES`, `GROUPS` (hub do celular) e a matriz `MODULES` — e elas divergiam em silêncio: módulo novo entrava na matriz e não aparecia em menu nenhum. Agora existe **um catálogo** (`src/lib/nav/areas.ts`, puro: só CHAVES, sem rótulo duplicado) resolvido no servidor por `montarMenu()` a partir de `MODULES` + `viewableNavHrefs` + o recorte da pizzaria. `tests/nav-catalogo.test.ts` cobra as duas direções (módulo com `nav` sem lugar no menu; chave de menu sem módulo) — as duas já tinham acontecido. **Sidebar removida no desktop**: `TopNav` com 7 áreas (Início, Tarefas, Operação, Pessoas, Performance, Relatórios, Administrativo) e mega menu em colunas; `sidebar.tsx`, `sidebar-state-provider.tsx`, `lib/sidebar-state.ts` e o teste saíram junto. A cor não mudou: bordô para ativo/seleção, semáforo só para status. **Busca global** deixou de ser botão e virou campo sempre aberto (`GlobalSearch`), lendo o catálogo — antes ela não alcançava as 18 telas de Configurações nem os relatórios. Ordem: **usar antes de configurar** ("troco" → Gestão de Troco, não Troco (denominações)). O ⌘K e o hub do celular passaram a ler o mesmo catálogo; o hub tinha 12 itens à mão e escondia Troco, Inventário, Atestados e tudo de Configurações de quem usa celular. **Seletor global "Toda a Rede"** (`TODA_A_REDE` = `'todas'`, o mesmo valor de `?unit=todas`): ⚠️ o padrão MUDOU — `getSelectedUnitId` devolvia a PRIMEIRA unidade (ordem alfabética) e não havia como pedir a rede; com mais de uma unidade no alcance o padrão passa a ser a rede. Trocar de unidade agora limpa `?unit=` da URL, senão o filtro explícito da tela venceria o chip e ele mentiria. **Favoritos** por aparelho em `localStorage` (toda leitura/escrita em try/catch; sem armazenamento a tela segue inteira), estrela no mega menu e no hub. A migalha do header saiu de `NAV_GROUPS` e passou a ler o menu montado, casando pelo caminho mais longo |
| — Central Operacional + Home do gerente (v1.99.0) | ✅ Concluído (etapa 2 da repaginação; `src/lib/dashboard/central.ts`, `components/dashboard/central-da-rede.tsx`, `acessos-rapidos.tsx`). O Dashboard virou **porta de entrada**, não painel: alertas → indicadores → unidades. **Todo número é composição do que os módulos já calculam** (`getUnitsOverview`, `getOccurrenceSummary`, `getConsolidadoFreelancers`, coletas de óleo, filas de pagamento/comandas/cancelamentos) — nenhuma tabela, cache ou conta paralela: painel que mantém a própria versão dos números discorda das telas em três meses. **Todo indicador tem `href` já filtrado** ("6 atrasadas" abre as 6; "recorrentes" abre o consolidado na semana com `rec=recorrentes`). **Desperdício mostra COBERTURA (dias lançados), não peso**: o peso vive em kg e em unidades ao mesmo tempo (categoria com medida `un`) e somar os dois daria número sem significado. **Alerta repetido vira um só** — 4 unidades sem óleo davam 4 linhas idênticas, e lista que se repete ensina a ignorar a lista. **"Unidades hoje" ordena por GRAVIDADE, não por nota** (a pergunta é onde entrar, não quem está ganhando; o ranking de metas continua em Metas). A central inteira obedece o seletor global via `resolveUnitFilter`. **Home do gerente** reordenada para o turno: atenção → meu dia → acessos rápidos → meta; `AcessosRapidos` usa os favoritos e, sem favorito, cai numa sugestão padrão (grade vazia pedindo configuração empurra curadoria para quem só quer trabalhar). ⚠️ Achado no navegador a 375px: com "Toda a Rede" o seletor ficou mais largo e passava **por cima** dos ícones do cabeçalho — o grupo da esquerda não encolhia (`min-w-0` em vez de `shrink-0`) |
| — Tablet sem navegação: correção (v1.99.1) | ✅ Concluído. 🔴 **Regressão da v1.98.0, que chegou a produção**: a `TopNav` nasceu `hidden lg:block` (≥1024px) e a `BottomNav` é `md:hidden` (<768px). Entre **768 e 1023px — todo tablet em retrato — o SGO ficava sem menu nenhum**. A sidebar removida era `md:block` e cobria exatamente essa faixa. `tsc`, lint e os 1243 testes passavam igual, e no monitor do desenvolvedor a tela está sempre acima de 1024px. A barra superior passou a `md:block`; no tablet o ícone e a seta de cada área saem (custavam ~150px e jogavam a sétima área, Administrativo, para fora), e a tira ganhou `overflow-x-auto` como rede de segurança — antes quem rolava de lado era a PÁGINA, o que o design system proíbe. A busca inline entrou em `md` junto com a lupa saindo, e o grupo da esquerda do cabeçalho virou `shrink` (não `flex-1`), o que fez "Toda a Rede" caber inteiro em 768px **e** em 375px. `tests/navegacao-breakpoints.test.ts` lê as CLASSES dos dois componentes e exige que a barra de cima entre no mesmo breakpoint em que a de baixo sai — verificado que ele falha ao reintroduzir o `lg`. **Varredura**: 54 rotas medidas em iframe de 768px, zero transbordo horizontal; as grades das telas existentes já respondiam bem no tablet, então a única correção necessária era a navegação |
| — Impressão saía em branco no sistema todo (v1.100.1) | ✅ Concluído. 🔴 **Em produção, em TODA tela**: gerar o PDF de qualquer relatório dava um arquivo com o número certo de páginas e nada escrito. Não era texto branco em papel branco — medido no PDF gerado pelo próprio Chrome: **73 páginas, ZERO operadores de desenho de texto**. A causa são as animações de entrada com `animation-fill-mode: both`, que é o certo na tela (o elemento já assume o primeiro quadro antes de a animação começar, e nada pisca). Só que o primeiro quadro de `sgo-page-emerge` é `opacity: 0` e o de `sgo-page-push` está deslocado 100% para fora da página; ao imprimir, o Chrome monta um documento novo, as animações voltam ao tempo zero e o papel não roda quadro nenhum — o `both` congela tudo no quadro inicial. Como `.sgo-page-enter` envolve toda tela do grupo (app) (`src/app/(app)/template.tsx`), valia para o sistema inteiro, em qualquer tema. `@media print` agora desliga `animation` e `transition` para **todo** elemento: o seletor é universal de propósito, porque listar as classes de hoje deixaria a armadilha armada para a próxima animação de entrada. Nenhum elemento depende de animação para ficar visível — os `opacity: 0` vivem só dentro de `@keyframes`. **Por que passou pela revisão**: na tela está perfeito, `opacity`/`transform` não mexem no layout (daí a contagem de páginas correta) e nenhum teste abre um PDF; `tests/impressao-sem-animacao.test.ts` trava a regra e foi verificado que falha sem o bloco. ⚠️ **Não confundir com a v1.95.0**: lá o defeito era outro (tema escuro pintando tinta clara em papel branco) e só atingia quem usa o sistema no escuro — esta segunda causa estava por baixo e atingia todo mundo. Verificação: 12 telas com PDF gerado por CDP, todas com texto (ex.: auditoria 0 → 104.028 operadores) |
| — Romaneio do separador dava 404 (v1.100.2) | ✅ Concluído. 🔴 **Em produção, para TODO separador e TODO pedido**: abrir o pedido, clicar em "Romaneio para imprimir" e cair em 404 — em cima do pedido que a pessoa tinha acabado de abrir. A página do romaneio do CD carregava por `getPedido`, que pede acesso à **unidade**; o separador **não tem unidade nenhuma** (desde a v1.92.0 ele é cadastrado por setor do CD, porque o CD atende a rede toda), então `canAccessUnit` recusava todos os pedidos e a página caía em `notFound()`. A tela de separação usa a porta certa (`getPedidoParaSeparar`, por setor) e o romaneio do GERENTE também (por unidade) — só o caminho do CD estava com a fechadura trocada, e nada em `tsc`/lint/testes falhava. **A correção não escreve uma segunda regra**: `getRomaneioDoCd` (em `separacao.ts`) pergunta a `getPedidoParaSeparar` e só então monta a folha — **quem abre o pedido imprime o romaneio dele, sempre**, por construção; uma regra equivalente escrita à parte envelheceria sozinha e traria o 404 de volta por outro caminho. O **conteúdo é a carga INTEIRA**, não só o setor de quem imprime: é o papel conferido na doca com a mercadoria já reunida, e o recorte por setor existe para ninguém separar a lista do colega por engano, não para esconder o que vai no mesmo caminhão. `getPedido` foi dividida em **porta** (unidade) e **montagem**; a parte sem escopo chama-se `carregarPedidoSemEscopoDeUnidade` — nome longo de propósito, porque ela não decide acesso e só serve a quem já decidiu por outra porta (hoje, só o CD). Gerente e supervisão seguem barrados por unidade, com teste. 5 casos novos em `tests/pedidos-separacao.integration.test.ts`, **3 deles verificados reprovando** o código anterior; conferido também no navegador com um separador real |
| 25.1 — Pizzas: fechamento por canal (v1.100.0) | ✅ Concluído. O formulário público deixou de pedir **sabor** e passou a ser **seis quantidades**: Teknisa e iFood × Gigante(35)/Grande(30)/Brotinho(25), com total por canal e TOTAL GERAL ao vivo. A lógica antiga (escolher sabor, tamanho, qtd e "Adicionar sabor") obrigava a montar uma linha por combinação — e a abertura por sabor não é o que a pizzaria tem na mão ao fechar o dia. **Estrutura**: `enum PizzaChannel` + `model PizzaClosingCount(closingId, channel, size, quantity)` com `@@unique([closingId, channel, size])`. Tabela à parte, e não seis colunas: canal novo vira LINHA, e "quanto de 35cm veio do iFood" sai de um group by. **Os totais NÃO são gravados** — total guardado é total que um dia discorda das parcelas. ⚠️ **Migração ADITIVA**: `pizza_closing_items` e `pizza_flavors` continuam de pé (derrubar tabela é destrutivo e precisa do Pedro), e o painel lê as DUAS fontes — só as contagens faria o histórico anterior sumir da tela sem ninguém ter apagado nada. A gravação apaga as linhas de sabor **do dia que reescreve**, para a mesma venda não contar duas vezes. **Venda antiga não é atribuída a canal nenhum**: é anterior à separação, e jogá-la no Teknisa inventaria origem. Por isso a barra Teknisa×iFood mede **entre os dois canais**, não sobre o total do período — senão um período com legado deixaria a barra quase vazia (defeito visto no navegador e corrigido). **Recusas**: `QUANTIDADES` (não-inteiro, negativo, >10.000 ou tipo errado — `quantidadeValida` exige `typeof number`, porque `Number('')` é 0 e viraria "vendeu zero" em silêncio) e `VAZIO` (as seis em zero: dia sem venda é dia sem fechamento, e gravá-lo afundaria a média por dia lançado). Campo **ausente** vira zero de propósito — faltar chave num link público é normal. Painel ganhou KPIs por canal, a comparação, o cruzamento tamanho×canal e a abertura por canal no histórico diário; "Sabores" virou cartão condicional, some quando não há legado no período |
| 25.2 — Massas e Desperdícios dentro do Controle de Pizzas (v1.111.0) | ✅ Concluído (`src/lib/pizzas/massas-tipos.ts` e `massas-calculo.ts` PUROS, `massas.ts` servidor, `massas-rota.ts`; `/api/pizzas/massas` pública por token + `/api/pizzas/massas/gestao` autenticada (`PIZZAS`/editar); `components/pizzas/massas-operacao.tsx` (UMA tela para as duas portas), `massas-painel.tsx`, `massas-gestao.tsx`; models `PizzaDoughBatch`/`PizzaDoughWaste`/`PizzaDoughCount`/`PizzaDoughChange`, enum `PizzaDoughWasteReason`). **Nada do Controle de Pizzas mudou**: o link `/pizzas/[token]` virou HOME com 4 botões e o fechamento de vendas foi para `/pizzas/[token]/vendas` intacto (mesmo formulário, mesmos 30 dias retroativos — decisão do Pedro: a regra "só hoje" vale para MASSAS); painel ganhou aba `?aba=massas` ao lado da de sempre. **O estoque NÃO é gravado**: `estoque inicial(D) = contagem física confirmada mais recente antes de D + tudo que aconteceu entre ela e D`; esperado = inicial + recebimentos − pizzas vendidas (1 pizza = 1 massa, lido de `PizzaClosingCount` + legado `PizzaClosingItem`) − desperdícios. Por isso a correção retroativa "recalcula tudo" sem código nenhum para isso (mesma decisão da variação do gás, v1.103.0). ⚠️ **`PizzaDoughCount.expectedAtClose` é o esperado CONGELADO no fechamento** — é o que distingue "Divergência de estoque" (não batia quando contou) de **"Divergência gerada por alteração retroativa"** (batia e deixou de bater); a contagem física nunca é tocada pelo recálculo, e a recontagem do próprio dia atualiza o congelado (é a nova verdade daquele fechamento), a correção retroativa de contagem não. **Regra de edição em UM lugar** (`podeMexerNoDia`): futuro nunca; link = só o dia operacional atual (`DIA_FECHADO`); sessão MANAGER/SUPERVISOR/ADMIN/CEO = qualquer dia, anterior exige motivo (`SEM_MOTIVO_ALTERACAO`, `retroactive=true`). **Nada some calado**: exclusão é lógica (`deletedAt`) e toda mudança, inclusive a do mesmo dia pelo link, vira `PizzaDoughChange` (antes/depois/quem/motivo; `changedById` nulo = link) + Auditoria `PIZZA_DOUGH_*` com `origem`. **Um recebimento = um lote**; saldo por lote é ESTIMATIVA FIFO por validade (a tela diz isso): descarte por validade abate o lote apontado, contagem física reconcilia (sobra sai do mais antigo, excesso vira "sem lote identificado" — inventar validade seria pior). Faixas 2 dias 🟡 / vence hoje e vencido 🔴, fixas. `EXPIRED` = PERDA POR VALIDADE, o resto = PRODUÇÃO; **todo desperdício consome 1 massa** (decisão do Pedro). Divergência pelo link notifica o gerente (`notifyUnitRole`); fechamento que bate não avisa. Foto do desperdício via multipart só pelo link; o volume é servido só a autenticado, então o link mostra "(com foto)" e o gerente vê no painel. 41 testes novos (21 puros com o cenário do pedido 30→32 + 20 de integração, 1 verificado revelando comportamento certo que eu previra errado: inserir um dia inteiro antes de hoje também torna HOJE retroativo). Verificado no navegador: link a 375px (recebimento, desperdício, fechamento com divergência) e painel + correção retroativa com motivo |
| 26 — Ticket Médio (v1.101.0) | ✅ Concluído (`/modulos/ticket-medio`, `src/lib/ticket-media/*`, `/configuracoes/ticket-medio`). Acompanhamento mensal das CHURRASCARIAS: importar a planilha do mês → conferir → gravar → cobrar quem falta → consolidar. ⚠️ **A planilha NÃO é um resumo**: a "Relação de Cupons SAT/NFC-e" do Teknisa tem UMA LINHA POR CUPOM (7.499 no arquivo real) e não traz total algum — as linhas "Total (Data:…)" vêm com as colunas de valor vazias. Quem soma é o SGO. **Cupom CANCELADO não entra** (eram 8, R$ 728,15: cancelado não é venda) e os descartes são contados por status e exibidos na prévia. Colunas achadas **pelo NOME** e não pela posição (relatório com título acima somaria a coluna errada em silêncio). **As três regras, num arquivo puro** (`calculo.ts`, usado pela tela, pela prévia, pelo consolidado e pelo cartão do Dashboard): receita = venda − desconto; ticket = receita ÷ cupons; **consolidado = Σreceita ÷ Σcupons e NUNCA a média dos tickets** — com A (100.000/2.000) e B (300.000/5.000) a média dá R$ 55,00 e o certo é R$ 57,14; a frase está escrita na tela e há teste. Acréscimo/gorjeta não entram na receita, mas a prévia avisa quando existem. **PARTICIPAÇÃO é configuração do módulo, não do cadastro**: o SGO não tem nada que diga "isto é churrascaria" (nome não serve, razão social é compartilhada, CNPJ não serve), então nada é inferido — o Admin marca em `/configuracoes/ticket-medio` e grava o **id real** da unidade; nenhum cadastro novo, nenhuma coluna na `Unit`. **Unidade nova nasce FORA** (é o que impede CD/lanchonete caírem no consolidado). A participação tem **VIGÊNCIA** (`TicketMediaParticipation`, `startsAt`/`endsAt` em "AAAA-MM"), e são VÁRIAS linhas por unidade: tirar do controle em setembro não muda o consolidado de janeiro, e sair-e-voltar não preenche o buraco do meio. **Importação em 2 etapas**: a prévia não grava e a confirmação **relê o arquivo** (devolver os totais da prévia seria confiar em número que passou pelo navegador). Recusa **mês trocado** lendo a data dos cupons (importar agosto como setembro é o engano mais provável, e o total sai plausível), unidade não participante, planilha sem as colunas, e **duplicidade nunca sobrescreve calada** — substituir exige a permissão de administrar o módulo e grava quem/quando/arquivo na linha e na Auditoria. Receita e ticket **não são colunas**: saem de cupons/venda/desconto. **Tela**: status do fechamento ANTES dos números (parcial nomeia quem falta), tabela por unidade com variação, consolidado, evolução em barras; a comparação com o mês anterior usa **só as unidades presentes nos dois meses**, senão uma importação faltando viraria "queda". O seletor diz **"Consolidado das churrascarias"** e não "Toda a Rede" — a rede tem CD, lanchonete e produtos. **O cartão do Dashboard fica no MÊS CORRENTE** (decisão do Pedro, mantida depois de eu entregar a alternativa de mostrar o último mês com lançamento); o mês vai escrito nele. Consequência a conhecer: a planilha de um mês só sai depois que ele fecha, então no começo de cada mês o cartão mostra "–" e "0 de N importadas" até a primeira entrar — é leitura correta do mês corrente e serve de cobrança; o que ele NÃO faz é escorregar para o mês passado. Teste trava as duas pontas. Permissões: `TICKET_MEDIA` (ver), `TICKET_MEDIA_IMPORT` (importar) e `CONFIG_TICKET_MEDIA` (participantes + substituir) são portas separadas na matriz — quem importa a planilha do mês não decide quem entra no consolidado. 59 testes |
| 27 — Estoque operacional (v1.105.0) | ✅ Etapa 1 de 4 (`/modulos/estoque`, `src/lib/stock/*`: `validade.ts` e `embalagem.ts` PUROS, `catalogo.ts`, `lotes.ts`, `query.ts`). Controle de prateleira pelo celular — bipar → informar o que encontrou → seguir. **NÃO é registro fiscal**, e o desenho inteiro sai daí: sem PDV integrado, o saldo é **DECLARADO** (entrada, contagem, ou a resposta ao alerta), nunca deduzido — inventar baixa por venda daria número exato e falso, pior que aproximado e assumido; toda mudança vira `StockMovement` com autor e hora. **A TRATATIVA é o coração** e é o que substitui o PDV: lote entrando na faixa, o SGO pergunta e grava a resposta (Lote finalizado · Ainda possui estoque — única que pede quantidade e mantém vivo · Descarte/perda · Transferido); sem ela, ou o gerente vira digitador de saída diária, ou o alerta repete a mesma pergunta para sempre sobre lote que acabou há um mês. Respondida uma faixa, a pergunta só volta quando o lote **CRUZA para a seguinte** (`lastReviewBand`) — alerta que se repete ensina a ignorar o alerta. **Validade por LOTE, não por produto**: o mesmo molho chega hoje vencendo em dezembro e na próxima entrega em fevereiro; duas entregas do MESMO lote **somam** numa linha (na prateleira é uma pilha, e duas linhas fariam responder duas vezes). ⚠️ **A faixa é CALCULADA na leitura, jamais gravada** (30 🟡/7 🟠/2 🔴/hoje 🔴/vencido ⛔) — o dia vira sozinho, e status guardado diria "vence em 7 dias" três semanas depois, plausível; é a mesma armadilha da variação do gás (v1.103.0). **`alertDays` é do PRODUTO** e decide só QUANDO entra no radar (15 dias × 6 meses não acendem juntos); a escalada seguinte é **fixa** de propósito — "crítico" tem de querer dizer o mesmo em toda unidade — e janela curta **não cala** faixa grave (a 2 dias é crítico, tenha a antecedência que tiver). **Embalagem** UN/FARDO/DISPLAY: saldo mora **sempre em unidades** (as duas grandezas seriam dois números que um dia discordam), tela mostra "5 fardos · 60 un" e a conversão aparece ANTES de salvar; o fator vai **congelado no lote**, senão corrigir o cadastro reescreveria quanto tinha um lote de três meses atrás. **Um cadastro, N códigos**: nenhuma tabela nova — `Product` já é o catálogo da rede e `ProductBarcode.code` já é `@unique`, então a duplicidade é impossível por construção; código novo oferece **vincular** em vez de criar 2º cadastro (que criaria dois saldos para a mesma prateleira), e a ordem é código exato primeiro, semelhança de nome depois (o gerente está de pé no estoque). O **gerente cadastra** produto novo, para a rede inteira. **SETOR DO CD SUGERIDO** (`setor-sugerido.ts` puro + `ai/produto-setor.ts`): Coca→Bebidas, chiclete→Bomboniere, coxinha→Salgados. **Duas camadas e a ordem é o desenho** — regra determinística resolve o romaneio inteiro na hora, de graça e SEM chave; a IA só no que sobra (perguntar à IA o que uma tabela responde em microssegundos gastaria segundos com o gerente de pé no estoque, e deixaria o cadastro dependente de rede). **A ORIGEM virou DERIVADA do setor**: com setor → `CD` e pedível; sem setor → `LOCAL`, só no estoque. Nascer `CD` SEM setor ficou **inalcançável** (era o defeito da v1.94.0: o item some da fila de todos os separadores sem erro). ⚠️ Regras calibradas contra o **romaneio real**, item por item (o teste É a lista), e ele ensinou três coisas: **o que a coisa é vence do que ela é feita** (coxinha de requeijão é salgado — por comprimento, "requeijão"(9) venceria "coxinha"(7), daí a PRIORIDADE conceito preparado>ingrediente); **o recipiente vence o conteúdo** (forminha p/ empada é descartável), mas **"embalagem" não é termo** porque ali é unidade de medida ("12 embalagens de brigadeiro") e roubaria a confeitaria; e **plural é a regra** (Bolos/Broas/Molhos de Pimenta/pães de sal) → raiz nos DOIS lados, com irregulares (pães→pão, descartáveis→descartável — este último também conserta o casamento do NOME do setor, que falhava por uma letra). ⚠️ **Nenhum nome de setor fixo no código**: hoje o banco tem 3 (Bebidas/Secos/Descartáveis) e o romaneio tem 8; cada conceito declara APELIDOS e casa com o cadastrado, então renomear "Secos"→"Despensa Cozinha" não quebra. Conceito sem setor cadastrado **não sugere** (apontar "mais ou menos parecido" manda para a fila de quem não tem o que fazer com ele). A IA recebe **lista fechada** e só responde um dos setores ou NENHUM, conferido contra a lista; inerte sem chave. ⚠️ **`ProductOrigin` ganhou `LOCAL`** — necessário porque o gerente bipando uma Coca do distribuidor não tem origem nem setor do CD, e produto do CD sem setor SOME da fila de todos os separadores (v1.94.0). **A esteira da Fábrica/CD não mudou**: o filtro é explícito em DOIS lugares (`listActiveProducts` = a tela, e `criarPedido` = a gravação) — só na tela deixaria a porta aberta para um item local virar `ProductRequest` de origem que nenhuma aba trata; os 3 testes disso foram **verificados reprovando** sem o filtro. Migração aditiva (2 tabelas, 3 enums, 3 colunas, 1 valor de enum), 156 testes. **Fora desta etapa, por combinação**: transferência entre unidades, gancho no recebimento da Fábrica/CD, painel consolidado e IA de cadastro por foto. **Sem valor em R$**: não existe custo por produto no SGO (nem catálogo nem notas, que têm valor total e não por item), e somar reais hoje seria inventar o número |
| — Mutirão do setor do CD (v1.106.0) + os 8 setores do romaneio (v1.107.0) | ✅ Concluído (`src/lib/stock/setor-em-lote.ts`, `components/products/mutirao-de-setores.tsx`, ações `setorIA`/`setorAplicar` em `/api/products`). Produto de origem CD **sem setor** cai em "Sem setor cadastrado" e **nenhum separador o enxerga** — some da fila de todo mundo sem erro nenhum; e como a importação por Excel escreve direto no Prisma (não passa por `upsertProduct`), o buraco se refaz a cada planilha, que é como mais de mil produtos chegaram lá. O aviso âmbar do catálogo ganhou **"Definir setores"**: abre a lista inteira com o setor **já proposto pela regra** da v1.105.0 e o **motivo ao lado** ("pela regra — coca cola"), porque corrigir 1.184 num modal por item é o caminho que ninguém termina. **"Usar a IA nos N sem proposta"** roda em **batelada** (20 por chamada, teto de 60 por clique): 1.184 idas à rede seriam caras, lentas e frágeis — falha no meio deixaria o mutirão pela metade sem ninguém saber onde parou —, e o teto também deixa o **custo visível** em vez de um botão que gasta valor imprevisto. **Nada aplica sem confirmação**, e cada linha oferece **"Deixar sem setor"**: sem setor é melhor que com o setor errado, porque o errado some numa fila que ninguém confere e o sem setor continua no aviso. ⚠️ **Três conferências no aplicar**, cada uma fechando um jeito de estragar dado EM ESCALA: só origem **CD** (um LOCAL ficaria com setor e sem ser pedível — estado que não quer dizer nada); só quem **ainda está sem setor** (não sobrescreve correção feita à mão com a tela aberta); só **setor ativo** (fila de setor desativado não é lida). O recusado é **contado** — "900 aplicados" escondendo 12 recusados esconderia justamente os que precisam de atenção. Gravação em **um `updateMany` por setor** (mil updates individuais segurariam a conexão por minutos numa transação). Auditoria `PRODUCT_SECTOR_BULK` **por setor** ("Bebidas: 214 itens"), não mil ids: a linha precisa ser legível meses depois. Na resposta da IA o **índice é conferido contra a faixa enviada** — índice fora da faixa ou repetido carimbaria o setor no produto errado, e o erro só apareceria na doca. 24 testes.**v1.106.0 também cria os 8 setores do romaneio** (`setores-padrao.ts`, chamado ao abrir Catálogo ou Setores do CD, padrão `ensureDefaultModels`): o classificador só aponta para setor que EXISTA e o cadastro tinha 3, então 5 conceitos ficavam reconhecidos e sem destino. ⚠️ **Cria só o que falta, e confere por CONCEITO e não por nome** — "Secos" e "DESPENSA COZINHA" são o mesmo lugar, e criar o segundo partiria a carga entre duas filas com dois separadores conferindo metade cada um (o pior tipo de duplicidade: ninguém vê erro, o caminhão é que chega incompleto). Reusa `conceitoCoberto` com os apelidos do classificador, então "Açougue" já cobre carnes; setor **inativo NÃO conta** (fila dele não é lida). Renomear depois não quebra (casa por apelido). Não roda no boot de propósito. Auditado em `CD_SECTOR_SEED`. A decisão é **pura** (`faltantesDoRomaneio`) e testada SEM banco: `cd_sectors` é tabela GLOBAL sem escopo por unidade, e limpar a tabela num teste derrubaria, em paralelo, os setores que os outros arquivos criaram — a falha apareceria no arquivo errado. **Escrevi o teste destrutivo primeiro e ele apagou os setores do banco de dev**; o desenho mudou por causa disso. 31 testes |
| 15 — Central de Comunicação | ✅ Concluído (comunicados supervisão/admin→gerentes com **confirmação de leitura obrigatória**; público-alvo por **unidade(s) + destinatários avulsos** (snapshot em `CommunicationRecipient`); mensagens ricas: **anexos foto/PDF, links, prioridade (normal/importante/urgente), fixar no topo, exigir resposta** (foto/comentário ao confirmar); **painel** de quem leu/pendente + barra de progresso; **histórico**; **conta na meta** — componente "Comunicados" peso único `AppSetting COMMUNICATION_META_WEIGHT` (no prazo pontua, atrasado neutro, vencido penaliza; `src/lib/communications/*`); notifica via Central de Notificações; Admin exclui via /api/admin/ops; **badge de pendentes no menu**, **lembrete automático** no scheduler (`notifyDueSoonCommunications`, ≤24h/vencido), **cobrança 1-clique** no painel (`/[id]/remind`)) |

## Como trabalhar
1. Leia a seção em `docs/especificacao.md`.
2. Plano curto antes de implementar; aguarde confirmação.
3. Um módulo por vez; testes para regras de negócio.
4. Commit por funcionalidade; tag ao concluir módulo; atualize esta tabela.
5. **A cada novo recurso/ajuste, atualize a central "Treinamento da Plataforma"** (`src/lib/guide.ts`, aba `/ajuda`) — guias por perfil (filtra por `user.role`; acessível pelo ícone 🎓 no header, em qualquer aparelho). Pedido do Pedro: a central deve refletir TUDO que for criado/ajustado.
6. **Biblioteca de modelos de checklist** (`ChecklistModel`/`ChecklistModelItem`, `src/lib/checklist-models.ts`): Admin CRUD em `/configuracoes/modelos`; seed padrão (rede Beija Flor, por setor/momento) em `checklist-models-seed.ts` via `ensureDefaultModels()`. Em `/configuracoes/checklists`, "Modelos prontos…" gera TaskTemplates nas unidades a partir dos modelos (`template.fromModels`). **Visualização** ao clicar no modelo; **Export/Import Excel (.xlsx via `xlsx`/SheetJS)** + **PDF (print)** para edição em lote (`/api/checklist-models/export|import`, upsert por nome, não destrutivo; `/configuracoes/modelos/imprimir`).

## Fluxo de entrega — gatilho "finaliza e sobe" (abre PR; CI + auto-merge publicam)
**Publicar o SGO = abrir um Pull Request para a `main`.** O CI (`.github/workflows/ci.yml`)
roda `tsc` + `lint` + testes (com Postgres efêmero) no PR; quando fica **verde**, o
**auto-merge** (`automerge.yml`) mescla na `main` sozinho e o `deploy.yml` publica em produção
— **sem ninguém clicar em Merge** (ver [[deploy-ci-cd]], `docs/deploy-auto-merge.md`).
**Trabalhe SEMPRE numa branch e abra PR. NÃO faça push direto na `main`** — a branch protection
bloqueia; o portão de qualidade é o CI verde.

Quando o usuário disser **"finaliza e sobe"** (ou equivalente), execute nesta ordem, **sem pedir confirmação** (o próprio pedido é a aprovação):

1. Garanta estar numa **branch de trabalho** (não na `main`): se estiver na main, `git checkout -b feat/descricao`.
2. `npx tsc --noEmit`
3. `npx next lint --no-cache`
4. `npm run lint:ds` — **o CI roda isto e é fácil esquecer** (foi o que derrubou o PR #78). São sete guards: hex/espaçamento fora da grade 8pt, classe de cor que não existe na paleta, foco de diálogo, ternário com os dois lados iguais, escala tipográfica, propriedade JSX presa em aspas e **componente cliente alcançando módulo de servidor** (este último quebra só no `next build`, ou seja, depois do merge — foi o que derrubou o deploy da v1.77.0).
5. `npm test`

**Se as quatro passarem:** commit em **PT-BR** (conventional commits: `feat:`/`fix:`/`refactor:`…, corpo explicando o porquê), suba o número em `src/lib/version.ts` se for mudança de verdade, `git push` da **branch**, e **abra o PR para a `main`** (`gh pr create` ou via API). O CI + auto-merge publicam quando ficar verde.

**Se qualquer uma falhar:** mostre o erro e **não suba nada** (sem commit, sem push, sem PR). Corrija ou reporte.

**Migrações de banco sobem sozinhas** no deploy (a esteira extrai `prisma/` da imagem publicada); o CI ainda as valida num Postgres de teste. Basta commitar o arquivo de `npx prisma migrate dev --name ...`. ⚠️ Migração **destrutiva** (drop/rename de coluna) precisa ser combinada com o Pedro antes: a reversão automática volta só o código, não o banco.

Rollback: aba **Actions → "Voltar versão (rollback)"** no GitHub.

Critérios de aprovação de cada verificação:
- `tsc`, `npm run lint:ds` e `npm test`: precisam sair com **exit 0** e zero falhas.
- `next lint`: **zero erros**. Avisos (`Warning`) pré-existentes não bloqueiam — hoje há 7, todos `no-img-element`. Se aparecer aviso **novo**, mencione antes de subir.
- **Confira pelo CÓDIGO DE SAÍDA, não contando linhas do texto.** Foi assim que o PR #83 caiu: contei os 7 avisos com `grep -c Warning` e não vi os 2 **erros** de `react-hooks/rules-of-hooks` no mesmo relatório. O certo é `npx next lint --no-cache; echo $?` (0 = passou) — vale para as quatro verificações.

## Comandos
- `docker compose up -d` — sobe o Postgres dedicado do SGO (dev)
- `npm run dev` — app em http://localhost:3100
- `npm run db:migrate` / `npm run db:seed` — schema + seeds
- `npm test` — testes (Vitest)
- `npm run lint:ds` — os sete guards de design system e de fronteira cliente/servidor (o CI roda; rode antes de abrir PR)
- `docker compose -f docker-compose.prod.yml up -d --build` — produção
