/**
 * Versão do aplicativo (exibida no rodapé/menu). Atualize a cada release e
 * registre o que mudou em CHANGELOG.md (raiz do projeto).
 * Convenção: v{maior}.{menor}.{correção} — correção = ajustes/bugs,
 * menor = melhorias/novas funções, maior = módulo novo grande.
 */
export const APP_VERSION = '1.3.3';
export const APP_VERSION_LABEL = `v${APP_VERSION}`;
